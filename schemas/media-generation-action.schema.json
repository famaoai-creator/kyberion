{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Media Generation Actuator Request",
  "type": "object",
  "required": ["action", "params"],
  "properties": {
    "action": {
      "enum": ["generate_image", "generate_video", "generate_music", "submit_generation", "get_generation_job", "wait_generation_job", "collect_generation_artifact", "capture_screen", "capture_focused_window", "record_screen", "run_workflow"]
    },
    "params": { "type": "object" }
  }
}
