// Public subpath bootstrap: direct consumers get the governed approval
// handler while ACP's implementation remains independent of the registry.
import './risky-op-registry.js';

export * from './acp-mediator.js';
