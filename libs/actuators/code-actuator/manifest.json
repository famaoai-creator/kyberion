{
  "actuator_id": "code-actuator",
  "version": "2.1.0",
  "description": "ADF-driven code analysis and refactoring pipeline engine",
  "contract_schema": "schemas/code-pipeline.schema.json",
  "resilience_tier": "adaptive_retry",
  "recovery_policy": {
    "fallback_strategy": "code_io_retry",
    "retry": {
      "maxRetries": 2,
      "initialDelayMs": 150,
      "maxDelayMs": 1200,
      "factor": 2,
      "jitter": true
    },
    "retryable_categories": ["resource_unavailable", "timeout"]
  },
  "capabilities": [
    { "op": "pipeline", "schema_ref": "schemas/code-pipeline.schema.json", "platforms": ["darwin", "linux", "win32"] },
    { "op": "reconcile", "platforms": ["darwin", "linux", "win32"] }
  ]
}
