name: Setup Kyberion
description: Checkout the repository, install the pinned toolchain, and install workspace dependencies.
inputs:
  node-version:
    description: Node.js version used by the job.
    required: false
    default: '24'
  install-playwright:
    description: Install the Chromium runtime required by browser-backed checks.
    required: false
    default: 'false'
runs:
  using: composite
  steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup pnpm
      uses: pnpm/action-setup@v4
      with:
        version: 11.15.1

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ inputs.node-version }}
        cache: pnpm

    - name: Install dependencies
      shell: bash
      run: pnpm install --frozen-lockfile

    - name: Install Playwright Chromium
      if: inputs.install-playwright == 'true'
      shell: bash
      run: pnpm exec playwright install --with-deps chromium
