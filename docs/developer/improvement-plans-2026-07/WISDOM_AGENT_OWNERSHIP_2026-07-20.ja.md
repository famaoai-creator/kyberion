# Wisdom / Agent / Orchestrator ownership matrix

この表は `wisdom-actuator/src/op-catalog.ts` の現在の公開op集合から生成した、契約整理時点のownership基準です。forwarder対象は境界と転送先を先に固定していますが、別actuatorの実transportをこの変更で複製せず、互換adapterの次PRで完成させます。

| Legacy op                                  | Canonical owner | Canonical op                               | Compatibility policy                                                         | Why this owner                                                                      |
| ------------------------------------------ | --------------- | ------------------------------------------ | ---------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- |
| `glob_files`                               | `file`          | `glob_files`                               | compatibility route -> file:list; adapter pending                            | file actuator owns generic filesystem I/O and its security boundary                 |
| `history_search`                           | `wisdom`        | `history_search`                           | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `knowledge_search`                         | `wisdom`        | `knowledge_search`                         | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `query`                                    | `wisdom`        | `query`                                    | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `read_file`                                | `file`          | `read_file`                                | compatibility route -> file:read_file; adapter pending                       | file actuator owns generic filesystem I/O and its security boundary                 |
| `read_json`                                | `file`          | `read_json`                                | compatibility route -> file:read_json; adapter pending                       | file actuator owns generic filesystem I/O and its security boundary                 |
| `shell`                                    | `terminal`      | `shell`                                    | compatibility route -> terminal:shell_command; adapter pending               | terminal actuator owns shell execution and command policy                           |
| `array_count`                              | `wisdom`        | `array_count`                              | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `json_query`                               | `wisdom`        | `json_query`                               | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `regex_extract`                            | `wisdom`        | `regex_extract`                            | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `regex_replace`                            | `wisdom`        | `regex_replace`                            | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `yaml_update`                              | `wisdom`        | `yaml_update`                              | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `a2a_fanout`                               | `wisdom`        | `perspective_fanout`                       | deprecated alias -> perspective_fanout                                       | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `perspective_fanout`                       | `wisdom`        | `perspective_fanout`                       | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `a2a_roleplay`                             | `wisdom`        | `counterparty_roleplay`                    | deprecated alias -> counterparty_roleplay                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `adjust_proposal`                          | `wisdom`        | `adjust_proposal`                          | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `audit_speaker_fairness`                   | `wisdom`        | `audit_speaker_fairness`                   | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `capture_intuition`                        | `wisdom`        | `capture_intuition`                        | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `compute_readiness_matrix`                 | `wisdom`        | `compute_readiness_matrix`                 | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `conduct_1on1`                             | `meeting`       | `conduct_1on1`                             | compatibility route -> meeting:conduct_1on1; adapter pending                 | meeting actuator owns meeting-derived records and session semantics                 |
| `curate_background_review`                 | `wisdom`        | `curate_background_review`                 | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `cross_critique`                           | `wisdom`        | `typed_cross_critique`                     | deprecated alias -> typed_cross_critique                                     | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `typed_cross_critique`                     | `wisdom`        | `typed_cross_critique`                     | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `decompose_into_tasks`                     | `orchestrator`  | `decompose_into_tasks`                     | compatibility route -> orchestrator:decompose_into_tasks; adapter pending    | orchestrator owns DAG ordering, runnable-task policy, and execution ledger          |
| `deploy_release`                           | `deployment`    | `deploy_release`                           | compatibility route -> deployment:deploy_release; adapter pending            | deployment actuator owns release side effects                                       |
| `distill`                                  | `wisdom`        | `distill`                                  | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `derive_test_inventory`                    | `wisdom`        | `derive_test_inventory`                    | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `emit_dissent_log`                         | `wisdom`        | `emit_dissent_log`                         | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `escalate_for_review`                      | `approval`      | `escalate_for_review`                      | compatibility route -> approval:request_review; adapter pending              | approval actuator owns approval transport and pending review state                  |
| `evaluate_architecture_ready`              | `wisdom`        | `evaluate_architecture_ready`              | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `evaluate_customer_signoff`                | `wisdom`        | `evaluate_customer_signoff`                | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `evaluate_decision_rights_approval`        | `approval`      | `evaluate_decision_rights_approval`        | compatibility route -> approval:evaluate_decision_rights; adapter pending    | approval actuator owns approval transport and pending review state                  |
| `evaluate_ensemble_convergence`            | `wisdom`        | `evaluate_ensemble_convergence`            | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `evaluate_qa_ready`                        | `wisdom`        | `evaluate_qa_ready`                        | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `evaluate_requirements_completeness`       | `wisdom`        | `evaluate_requirements_completeness`       | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `evaluate_simulation_quality`              | `wisdom`        | `evaluate_simulation_quality`              | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `evaluate_task_plan_ready`                 | `wisdom`        | `evaluate_task_plan_ready`                 | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `execute_self_action_items`                | `wisdom`        | `execute_self_action_items`                | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `execute_task_plan`                        | `orchestrator`  | `execute_task_plan`                        | compatibility route -> orchestrator:run_execution_plan_set; adapter pending  | orchestrator owns DAG ordering, runnable-task policy, and execution ledger          |
| `extract_action_items`                     | `meeting`       | `extract_action_items`                     | compatibility route -> meeting:extract_action_items; adapter pending         | meeting actuator owns meeting-derived records and session semantics                 |
| `extract_design_spec`                      | `modeling`      | `extract_design_spec`                      | compatibility route -> modeling:extract_design_spec; adapter pending         | modeling actuator owns SDLC artifact extraction and gates                           |
| `extract_dissent_signals`                  | `wisdom`        | `extract_dissent_signals`                  | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `extract_requirements`                     | `modeling`      | `extract_requirements`                     | compatibility route -> modeling:extract_requirements; adapter pending        | modeling actuator owns SDLC artifact extraction and gates                           |
| `extract_test_plan`                        | `modeling`      | `extract_test_plan`                        | compatibility route -> modeling:extract_test_plan; adapter pending           | modeling actuator owns SDLC artifact extraction and gates                           |
| `find_slides_by_owner`                     | `media`         | `find_slides_by_owner`                     | compatibility route -> media:pptx_filter_slides; adapter pending             | media actuator owns presentation/media parsing and comparison                       |
| `fork_branches`                            | `wisdom`        | `fork_branches`                            | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `generate_facilitation_script`             | `meeting`       | `generate_facilitation_script`             | compatibility route -> meeting:generate_facilitation_script; adapter pending | meeting actuator owns meeting-derived records and session semantics                 |
| `generate_reminder_message`                | `meeting`       | `generate_reminder_message`                | compatibility route -> meeting:generate_reminder_message; adapter pending    | meeting actuator owns meeting-derived records and session semantics                 |
| `inject_prior_knowledge`                   | `wisdom`        | `inject_prior_knowledge`                   | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `knowledge_export`                         | `wisdom`        | `knowledge_export`                         | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `knowledge_import`                         | `wisdom`        | `knowledge_import`                         | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `knowledge_inject`                         | `wisdom`        | `knowledge_inject`                         | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `log`                                      | `wisdom`        | `log`                                      | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `peer_advice`                              | `wisdom`        | `peer_advice`                              | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `pptx_diff`                                | `media`         | `pptx_diff`                                | compatibility route -> media:pptx_diff; adapter pending                      | media actuator owns presentation/media parsing and comparison                       |
| `react_loop`                               | `wisdom`        | `reasoning_loop`                           | deprecated alias -> reasoning_loop                                           | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `reasoning`                                | `wisdom`        | `reasoning`                                | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `recommend`                                | `wisdom`        | `recommend`                                | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `register_presentation_preference_profile` | `wisdom`        | `register_presentation_preference_profile` | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `render_hypothesis_report`                 | `wisdom`        | `render_hypothesis_report`                 | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `resolve_hypothesis_conflict`              | `wisdom`        | `resolve_hypothesis_conflict`              | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `simulate_all`                             | `wisdom`        | `simulate_all`                             | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `simulate_all_ensemble`                    | `wisdom`        | `simulate_all_ensemble`                    | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `stakeholder_grid_sort`                    | `wisdom`        | `stakeholder_grid_sort`                    | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `synthesize_counterparty_persona`          | `wisdom`        | `synthesize_counterparty_persona`          | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `task_plan_to_next_tasks`                  | `wisdom`        | `task_plan_to_next_tasks`                  | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `tool_use`                                 | `wisdom`        | `propose_tool_calls`                       | deprecated alias -> propose_tool_calls                                       | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `track_pending_action_items`               | `wisdom`        | `track_pending_action_items`               | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `transcribe_audio`                         | `voice`         | `transcribe_audio`                         | compatibility route -> voice:transcribe; adapter pending                     | voice actuator owns speech/audio processing                                         |
| `uncertainty_gate`                         | `wisdom`        | `uncertainty_gate`                         | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `write_artifact`                           | `file`          | `write_artifact`                           | compatibility route -> file:write_artifact; adapter pending                  | file actuator owns generic filesystem I/O and its security boundary                 |
| `write_file`                               | `file`          | `write_file`                               | compatibility route -> file:write_file; adapter pending                      | file actuator owns generic filesystem I/O and its security boundary                 |
| `if`                                       | `wisdom`        | `if`                                       | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
| `while`                                    | `wisdom`        | `while`                                    | canonical                                                                    | knowledge, governed reasoning, or decision-support; no runtime side effect boundary |
