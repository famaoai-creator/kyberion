# Kyberion Configuration Surface

> Generated from `knowledge/product/governance/env-registry.json` by `pnpm generate:env-registry` — do not edit by hand.
> `pnpm run check -- --scope full --only env-registry` (included in `pnpm validate`) fails when code references an unregistered `KYBERION_*` variable.

## What belongs where

- **Environment variables**: secrets, environment-specific endpoints/paths, and feature flags. Validated at startup by `libs/core/env-validator.ts` (warn by default; missing required values are errors).
- **Registry fields**: `required: true` is reserved for an unconditional startup prerequisite and must also be `documented: true`; conditional capability prerequisites stay in the environment manifests. `documented: false` is an honest discovery state, not a runtime failure.
- **Secrets**: may be documented by name and purpose, but their values never belong in this registry and they cannot be marked required here. Secret requirements are enforced by the selected capability or command.
- **Config files (`knowledge/product/**`)**: policy thresholds (SA plans), model IDs (IP-13), catalogs and vocabularies. These need review, diffing, and schema validation — not per-host overrides.

Copy [`env.example`](./env.example) to `.env` at the repo root for local overrides (the example is generated here because root dotfiles are write-protected by the policy engine).

## secret (33)

| Variable                                  | Type   | Required | Description                                                                                                                                                            |
| ----------------------------------------- | ------ | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `KYBERION_A2A_SECRET`                     | string | no       | Shared secret used to sign and verify A2A envelopes; values stay in the secret store.                                                                                  |
| `KYBERION_AGENT_RUNTIME_SUPERVISOR_TOKEN` | string | no       | Shared token for newline-JSON agent runtime supervisor RPC.                                                                                                            |
| `KYBERION_AGY_SDK_API_KEY`                | string | no       | Internal bridge transport for the Antigravity SDK API key; prefer GEMINI_API_KEY or GOOGLE_API_KEY in the parent environment.                                          |
| `KYBERION_API_TOKEN`                      | string | no       | Bearer token for authenticated surface/API requests.                                                                                                                   |
| `KYBERION_AUDIT_CHAIN_KEY`                | string | no       | HMAC key used to authenticate the append-only audit chain; values stay in the secret store.                                                                            |
| `KYBERION_BACKUP_PASSPHRASE`              | string | no       | Passphrase used to encrypt and restore backup archives; keep it in an external secret manager and never store it in the archive or registry.                           |
| `KYBERION_BLUEBUBBLES_PASSWORD`           | string | no       | Password used to authenticate requests to the BlueBubbles server.                                                                                                      |
| `KYBERION_BLUEBUBBLES_WEBHOOK_SECRET`     | string | no       | Shared secret used to authenticate inbound BlueBubbles webhook requests.                                                                                               |
| `KYBERION_GEMINI_VIDEO_API_KEY`           | string | no       | API key used by the explicit Google Veo video-generation backend.                                                                                                      |
| `KYBERION_GROK_API_KEY`                   | string | no       | xAI Grok API key for the grok-api reasoning backend. `XAI_API_KEY` is also accepted for compatibility.                                                                 |
| `KYBERION_LLAMACPP_KEY`                   | string | no       | API key for the llama.cpp OpenAI-compatible backend, when authentication is enabled.                                                                                   |
| `KYBERION_LMSTUDIO_KEY`                   | string | no       | API key for the LM Studio OpenAI-compatible backend, when authentication is enabled.                                                                                   |
| `KYBERION_LOCAL_LLM_KEY`                  | string | no       | API key for the local LLM endpoint.                                                                                                                                    |
| `KYBERION_LOCALADMIN_TOKEN`               | string | no       | Bearer token for the chronos localadmin control surface.                                                                                                               |
| `KYBERION_LOCALAI_KEY`                    | string | no       | API key for the LocalAI OpenAI-compatible backend, when authentication is enabled.                                                                                     |
| `KYBERION_MANIFEST_SIGNING_KEY`           | string | no       | HMAC-SHA256 key for environment manifest signatures. When set, loadEnvironmentManifest enforces valid signatures fail-closed; sign manifests with pnpm manifests:sign. |
| `KYBERION_MEET_EXTENSION_TOKEN`           | string | no       | Pre-shared token used to authenticate the Chrome meeting extension bridge.                                                                                             |
| `KYBERION_MESH_SHARED_SECRET`             | string | no       | Shared secret used to authenticate mesh delivery and peer coordination.                                                                                                |
| `KYBERION_MINIMAX_VIDEO_API_KEY`          | string | no       | API key used by the explicit MiniMax Hailuo video-generation backend.                                                                                                  |
| `KYBERION_MLX_KEY`                        | string | no       | API key for the MLX OpenAI-compatible backend, when authentication is enabled.                                                                                         |
| `KYBERION_NEMOTRON_KEY`                   | string | no       | API key for the Nemotron OpenAI-compatible backend, when authentication is enabled.                                                                                    |
| `KYBERION_OLLAMA_KEY`                     | string | no       | API key for the Ollama OpenAI-compatible backend, when authentication is enabled.                                                                                      |
| `KYBERION_OPENAI_VIDEO_API_KEY`           | string | no       | API key used by the explicit OpenAI Sora video-generation backend.                                                                                                     |
| `KYBERION_OPENROUTER_KEY`                 | string | no       | OpenRouter API key. `OPENROUTER_API_KEY` is also accepted for compatibility.                                                                                           |
| `KYBERION_PEER_KEY_REF`                   | string | no       | Secret reference recorded in peer enrollment; it identifies the secret without storing its value.                                                                      |
| `KYBERION_PEER_SHARED_SECRET`             | string | no       | HMAC shared secret used to authenticate peer messaging.                                                                                                                |
| `KYBERION_RUNWAY_API_KEY`                 | string | no       | API key used by the explicit Runway video-generation backends.                                                                                                         |
| `KYBERION_SHARE_GRANTS_HMAC_KEY`          | string | no       | HMAC key used to sign and verify share-grant records.                                                                                                                  |
| `KYBERION_SMTP_PASS`                      | string | no       | SMTP password or app token used by the email actuator; provide it through the secret store and keep it out of logs and configuration artifacts.                        |
| `KYBERION_SOVEREIGN_SECRET`               | string | no       | Secret used by the sovereign pulse guard to sign and verify control payloads.                                                                                          |
| `KYBERION_SUDO_KEY`                       | string | no       | Key used by the system prelude for privileged sudo coordination; values stay in the secret store.                                                                      |
| `KYBERION_TERMINAL_TOKEN`                 | string | no       | Bearer token used to authenticate the terminal bridge.                                                                                                                 |
| `KYBERION_VLLM_KEY`                       | string | no       | API key for the vLLM OpenAI-compatible backend, when authentication is enabled.                                                                                        |

## path (87)

| Variable                                                         | Type | Required | Description                                                                                                                                                                         |
| ---------------------------------------------------------------- | ---- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `KYBERION_AGENT_RUNTIME_SUPERVISOR_LOCK_PATH`                    | path | no       | Lock-file path used to prevent duplicate agent runtime supervisor daemons; keep it in the governed runtime area.                                                                    |
| `KYBERION_AGENT_RUNTIME_SUPERVISOR_SOCKET_PATH`                  | path | no       | Unix-domain socket path for local agent runtime supervisor RPC; keep it under the governed runtime area.                                                                            |
| `KYBERION_AGY_CLI_BIN`                                           | path | no       | Optional path override for agy cli bin; keep it inside governed repository or runtime storage.                                                                                      |
| `KYBERION_AGY_CLI_LOG_FILE`                                      | path | no       | Optional path override for agy cli log file; keep it inside governed repository or runtime storage.                                                                                 |
| `KYBERION_AGY_SDK_PYTHON`                                        | path | no       | Python 3.10+ interpreter used by the official google-antigravity SDK bridge.                                                                                                        |
| `KYBERION_ANTIGRAVITY_CLI_BIN`                                   | path | no       | Optional path override for antigravity cli bin; keep it inside governed repository or runtime storage.                                                                              |
| `KYBERION_AUTONOMOUS_OPS_POLICY_PATH`                            | path | no       | Optional path override for the autonomous-operations policy catalog; the selected catalog remains schema-validated before an operation is admitted.                                 |
| `KYBERION_CLAUDE_CLI_BIN`                                        | path | no       | Path override for the claude CLI binary.                                                                                                                                            |
| `KYBERION_CODEX_CLI_BIN`                                         | path | no       | Explicit executable path for the Codex CLI query backend; when unset, the backend resolves an acceptable Codex binary from the host.                                                |
| `KYBERION_COMFY_OUTPUT_DIR`                                      | path | no       | Optional path override for comfy output dir; keep it inside governed repository or runtime storage.                                                                                 |
| `KYBERION_CONFIDENTIAL_OPERATOR_LEARNING_DISPATCH_REGISTRY_PATH` | path | no       | Optional path override for confidential operator learning dispatch registry path; keep it inside governed repository or runtime storage.                                            |
| `KYBERION_CONTEXTUAL_INTENT_LEARNING_PATH`                       | path | no       | Optional path override for contextual intent learning path; keep it inside governed repository or runtime storage.                                                                  |
| `KYBERION_CONTEXTUAL_INTENT_MEMORY_PATH`                         | path | no       | Optional path override for contextual intent memory path; keep it inside governed repository or runtime storage.                                                                    |
| `KYBERION_COPILOT_CLI_BIN`                                       | path | no       | Optional path override for copilot cli bin; keep it inside governed repository or runtime storage.                                                                                  |
| `KYBERION_CURATION_ARCHIVE_HISTORY_PATH`                         | path | no       | Optional path override for curation archive history path; keep it inside governed repository or runtime storage.                                                                    |
| `KYBERION_CURATION_REPORT_PATH`                                  | path | no       | Optional output path for the knowledge curation report; use a governed shared runtime or report location rather than a tenant-unscoped data directory.                              |
| `KYBERION_CURATION_SLO_CONFIG_PATH`                              | path | no       | Optional path override for curation slo config path; keep it inside governed repository or runtime storage.                                                                         |
| `KYBERION_CURATION_TAXONOMY_PATH`                                | path | no       | Optional path override for curation taxonomy path; keep it inside governed repository or runtime storage.                                                                           |
| `KYBERION_DELEGATION_NOTIFICATIONS_PATH`                         | path | no       | Override path for durable delegation notifications; keep it inside the governed shared runtime area.                                                                                |
| `KYBERION_DELEGATION_STORE_DIR`                                  | path | no       | Directory for durable delegation task records; keep it within governed shared runtime storage.                                                                                      |
| `KYBERION_DELEGATION_TRACE_PATH`                                 | path | no       | Override path for delegated-task trace records used for observability and replay; keep it in governed runtime storage.                                                              |
| `KYBERION_DEPLOY_CONFIG_PATH`                                    | path | no       | Optional path override for deploy config path; keep it inside governed repository or runtime storage.                                                                               |
| `KYBERION_EGRESS_POLICY_PATH`                                    | path | no       | Optional path to the governed egress-policy catalog; keep it in an approved knowledge or deployment configuration location.                                                         |
| `KYBERION_GEMINI_CLI_BIN`                                        | path | no       | Optional path override for gemini cli bin; keep it inside governed repository or runtime storage.                                                                                   |
| `KYBERION_GROK_CLI_BIN`                                          | path | no       | Optional path override for grok cli bin; keep it inside governed repository or runtime storage.                                                                                     |
| `KYBERION_HINTS_ARCHIVE_DIR`                                     | path | no       | Optional path override for hints archive dir; keep it inside governed repository or runtime storage.                                                                                |
| `KYBERION_HINTS_PATH`                                            | path | no       | Optional path override for hints path; keep it inside governed repository or runtime storage.                                                                                       |
| `KYBERION_INTENT_CONTRACT_MEMORY_EXPORT_DIR`                     | path | no       | Override for exported intent-contract memory artifacts; the global default is active/shared/exports/intent-contract-memory and tenant runs remain physically scoped.                |
| `KYBERION_INTENT_CONTRACT_MEMORY_REPORT_PATH`                    | path | no       | Override for the intent-contract memory sync report; the global default is active/shared/runtime/reports/intent-contract-memory-sync-latest.json.                                   |
| `KYBERION_INTENT_CONTRACT_MEMORY_RUNTIME_PATH`                   | path | no       | Override for the global intent-contract memory runtime file; relative values resolve from the repository root and tenant-scoped runs use their physical scope path.                 |
| `KYBERION_INTENT_CONTRACT_MEMORY_SCHEMA_PATH`                    | path | no       | Override for the intent-contract memory schema used by the sync command; the default is knowledge/product/schemas/intent-contract-memory.schema.json.                               |
| `KYBERION_INTENT_CONTRACT_MEMORY_SEED_PATH`                      | path | no       | Override for the governance seed used by intent-contract memory sync; tenant-scoped memory cannot be promoted into the global seed without the governed brokered path.              |
| `KYBERION_KI_CACHE_DIR`                                          | path | no       | Optional path override for ki cache dir; keep it inside governed repository or runtime storage.                                                                                     |
| `KYBERION_KNOWLEDGE_DELIVERY_DIR`                                | path | no       | Override for knowledge feedback delivery logs; the default is active/shared/runtime/feedback-loop/knowledge-delivery and scoped runs append under their tenant or mission path.     |
| `KYBERION_KNOWLEDGE_FEEDBACK_DIR`                                | path | no       | Override for scoped human-feedback and knowledge-gap JSONL storage (default active/shared/runtime/feedback-loop).                                                                   |
| `KYBERION_KNOWLEDGE_FEEDBACK_POLICY_PATH`                        | path | no       | Override for the knowledge feedback policy catalog; unsafe paths are rejected and the canonical product governance policy remains the fallback.                                     |
| `KYBERION_KNOWLEDGE_SCOPE_HEALTH_HISTORY_PATH`                   | path | no       | Override for the knowledge-scope health history report; the default is active/shared/runtime/feedback-loop/knowledge-scope-health.json.                                             |
| `KYBERION_KNOWLEDGE_SCOPE_RECONCILIATION_PATH`                   | path | no       | Override for the knowledge-scope reconciliation report; the default is active/shared/runtime/reports/knowledge-scope-reconciliation-latest.json.                                    |
| `KYBERION_KNOWLEDGE_USAGE_PATH`                                  | path | no       | Override for aggregated knowledge usage and yield data; the default is active/shared/runtime/feedback-loop/knowledge-usage/usage.json and scoped reads preserve tenant containment. |
| `KYBERION_MEDIA_BACKEND_REGISTRY_PATH`                           | path | no       | Optional path override for media backend registry path; keep it inside governed repository or runtime storage.                                                                      |
| `KYBERION_MEMORY_QUEUE_PATH`                                     | path | no       | Optional path override for memory queue path; keep it inside governed repository or runtime storage.                                                                                |
| `KYBERION_MESH_HUB_OBSERVABILITY_ROOT`                           | path | no       | Optional path override for mesh hub observability root; keep it inside governed repository or runtime storage.                                                                      |
| `KYBERION_MESH_HUB_RUNTIME_ROOT`                                 | path | no       | Optional path override for mesh hub runtime root; keep it inside governed repository or runtime storage.                                                                            |
| `KYBERION_MISSION_TASK_TRACE_DIR`                                | path | no       | Optional path override for mission task trace dir; keep it inside governed repository or runtime storage.                                                                           |
| `KYBERION_OAUTH_CALLBACK_PATH`                                   | path | no       | Optional path override for oauth callback path; keep it inside governed repository or runtime storage.                                                                              |
| `KYBERION_OPERATOR_LEARNING_DISPATCH_REGISTRY_PATH`              | path | no       | Optional path override for operator learning dispatch registry path; keep it inside governed repository or runtime storage.                                                         |
| `KYBERION_PERSONAL_OPERATOR_LEARNING_DISPATCH_REGISTRY_PATH`     | path | no       | Optional path override for personal operator learning dispatch registry path; keep it inside governed repository or runtime storage.                                                |
| `KYBERION_PERSONAL_PRESENTATION_PREFERENCE_REGISTRY_PATH`        | path | no       | Optional path override for personal presentation preference registry path; keep it inside governed repository or runtime storage.                                                   |
| `KYBERION_PERSONAL_SURFACE_QUERY_CONFIG_PATH`                    | path | no       | Optional path override for personal surface query config path; keep it inside governed repository or runtime storage.                                                               |
| `KYBERION_PERSONAL_VOICE_PROFILE_REGISTRY_PATH`                  | path | no       | Optional path override for personal voice profile registry path; keep it inside governed repository or runtime storage.                                                             |
| `KYBERION_PRESENCE_AVATAR_PROFILES_PATH`                         | path | no       | Optional path override for presence avatar profiles path; keep it inside governed repository or runtime storage.                                                                    |
| `KYBERION_PRESENTATION_PREFERENCE_REGISTRY_PATH`                 | path | no       | Optional path override for presentation preference registry path; keep it inside governed repository or runtime storage.                                                            |
| `KYBERION_PROJECT_ROOT`                                          | path | no       | Optional path override for project root; keep it inside governed repository or runtime storage.                                                                                     |
| `KYBERION_PROVIDER_EGRESS_POLICY_PATH`                           | path | no       | Optional path override for provider egress policy path; keep it inside governed repository or runtime storage.                                                                      |
| `KYBERION_PROVIDER_HEALTH_STATE_PATH`                            | path | no       | Override for the persisted provider-health state file (default active/shared/runtime/provider-health.json). Mainly for tests.                                                       |
| `KYBERION_PYTHON`                                                | path | no       | Python interpreter override for python-backed bridges.                                                                                                                              |
| `KYBERION_PYTHON_BIN`                                            | path | no       | Python interpreter path override (legacy alias of KYBERION_PYTHON call sites).                                                                                                      |
| `KYBERION_ROOT`                                                  | path | no       | Repository root override for path resolution (defaults to the detected repo root).                                                                                                  |
| `KYBERION_SCOPE_ENV_PATH`                                        | path | no       | Override for the persisted operator scope binding file (default active/shared/runtime/scope.env).                                                                                   |
| `KYBERION_SECURITY_QUARANTINE_DIR`                               | path | no       | Optional path override for security quarantine dir; keep it inside governed repository or runtime storage.                                                                          |
| `KYBERION_SERVICE_ENDPOINTS_DIR`                                 | path | no       | Optional path override for service endpoints dir; keep it inside governed repository or runtime storage.                                                                            |
| `KYBERION_SERVICE_ENDPOINTS_PATH`                                | path | no       | Optional path override for service endpoints path; keep it inside governed repository or runtime storage.                                                                           |
| `KYBERION_SERVICE_PRESETS_DIR`                                   | path | no       | Optional path override for service presets dir; keep it inside governed repository or runtime storage.                                                                              |
| `KYBERION_SERVICE_RUNTIME_POLICY_PATH`                           | path | no       | Optional path override for service runtime policy path; keep it inside governed repository or runtime storage.                                                                      |
| `KYBERION_SERVICE_RUNTIME_REGISTRY_PATH`                         | path | no       | Optional path override for service runtime registry path; keep it inside governed repository or runtime storage.                                                                    |
| `KYBERION_SHARE_GRANTS_PATH`                                     | path | no       | Optional path override for share grants path; keep it inside governed repository or runtime storage.                                                                                |
| `KYBERION_SHELL_COMMAND_POLICY_PATH`                             | path | no       | Optional path to the shell-command policy catalog used for command admission and injection-suspected restrictions; the policy is loaded through the governed catalog boundary.      |
| `KYBERION_STT_MODEL_DIR`                                         | path | no       | Optional repository-safe model directory used by local STT adapter selection, including faster-whisper and Whisper model availability checks.                                       |
| `KYBERION_SURFACE_QUERY_CONFIG_PATH`                             | path | no       | Optional path override for surface query config path; keep it inside governed repository or runtime storage.                                                                        |
| `KYBERION_TASK_GRANTS_PATH`                                      | path | no       | Optional path override for task grants path; keep it inside governed repository or runtime storage.                                                                                 |
| `KYBERION_TASK_SCENARIO_DIR`                                     | path | no       | Optional path override for task scenario dir; keep it inside governed repository or runtime storage.                                                                                |
| `KYBERION_TENANT_DESIGN_ROOT`                                    | path | no       | Optional governed root for tenant design assets used by the Chronos tenant-design route; viewer-resolved tenant scope still controls which customer data is selected.               |
| `KYBERION_TEST_OBSERVABILITY_DIR`                                | path | no       | Optional path override for test observability dir; keep it inside governed repository or runtime storage.                                                                           |
| `KYBERION_TOOL_ACTUATOR_ROUTING_POLICY_PATH`                     | path | no       | Optional path override for tool actuator routing policy path; keep it inside governed repository or runtime storage.                                                                |
| `KYBERION_TOOL_RUNTIME_POLICY_PATH`                              | path | no       | Optional path override for tool runtime policy path; keep it inside governed repository or runtime storage.                                                                         |
| `KYBERION_TOOL_RUNTIME_REGISTRY_PATH`                            | path | no       | Optional path override for tool runtime registry path; keep it inside governed repository or runtime storage.                                                                       |
| `KYBERION_VIDEO_COMPOSITION_TEMPLATE_REGISTRY_PATH`              | path | no       | Optional path override for video composition template registry path; keep it inside governed repository or runtime storage.                                                         |
| `KYBERION_VIDEO_RENDER_RUNTIME_POLICY_PATH`                      | path | no       | Optional path override for video render runtime policy path; keep it inside governed repository or runtime storage.                                                                 |
| `KYBERION_VOICE_BRIDGE_PATH`                                     | path | no       | Optional path to the Python voice-learning bridge; when unset, the bundled voice-actuator bridge is used.                                                                           |
| `KYBERION_VOICE_ENGINE_REGISTRY_DIR`                             | path | no       | Directory override for the split voice-engine registry files; used with the canonical registry path when present.                                                                   |
| `KYBERION_VOICE_ENGINE_REGISTRY_PATH`                            | path | no       | Explicit voice-engine registry path; the selected file is validated against the voice-engine schema before use.                                                                     |
| `KYBERION_VOICE_HUB_TTS_CONFIG_PATH`                             | path | no       | Override for the voice-hub text-to-speech language configuration; the default is knowledge/product/presence/voice-hub-tts.json.                                                     |
| `KYBERION_VOICE_OUTPUT_DIR`                                      | path | no       | Directory for generated Python voice artifacts; the default is active/shared/tmp/voice-out under the current repository.                                                            |
| `KYBERION_VOICE_PROFILE_REGISTRY_DIR`                            | path | no       | Directory override for split voice-profile registry files; canonical overlays remain enabled when no explicit registry file is selected.                                            |
| `KYBERION_VOICE_PROFILE_REGISTRY_PATH`                           | path | no       | Explicit base voice-profile registry path; selecting it disables the automatic personal and customer overlay lookup.                                                                |
| `KYBERION_VOICE_RUNTIME_POLICY_PATH`                             | path | no       | Override for the voice runtime policy catalog; the default is knowledge/product/governance/voice-runtime-policy.json and invalid input falls back conservatively.                   |
| `KYBERION_VOICE_SAMPLE_INGESTION_POLICY_PATH`                    | path | no       | Override for the voice sample-ingestion policy catalog; the selected file is schema-validated before profile registration.                                                          |

## flag (20)

| Variable                                           | Type                   | Required | Description                                                                                                                                                                                                                                 |
| -------------------------------------------------- | ---------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `KYBERION_ALLOW_FILE_SECRETS`                      | boolean                | no       | Acknowledge the plaintext file-secret fallback when the OS keychain is unavailable (warn-phase; see AC-05).                                                                                                                                 |
| `KYBERION_ALLOW_LOCAL_NETWORK`                     | boolean                | no       | Allow explicitly opted-in development flows to reach local or private network addresses; disabled by default.                                                                                                                               |
| `KYBERION_ALLOW_STUB_FALLBACK`                     | boolean                | no       | Keep the reasoning stub fallback when the configured provider chain is unavailable; disabled by default.                                                                                                                                    |
| `KYBERION_ALLOW_TEST_NOTIFICATIONS`                | boolean                | no       | Allow operator notification delivery during tests; test-only opt-in.                                                                                                                                                                        |
| `KYBERION_ALLOW_UNAUTH_REMOTE`                     | boolean                | no       | Allow unauthenticated remote Chronos readonly access only when no API or localadmin token is configured; disabled by default.                                                                                                               |
| `KYBERION_ALLOW_UNSAFE_CLI`                        | boolean                | no       | Allow the service actuator to execute CLI commands; disabled by default.                                                                                                                                                                    |
| `KYBERION_ALLOW_UNSAFE_JS`                         | boolean                | no       | Allow code or system actuators to evaluate JavaScript; disabled by default.                                                                                                                                                                 |
| `KYBERION_ALLOW_UNSAFE_SHELL`                      | boolean                | no       | Allow shell execution by code, system, or network actuators; disabled by default.                                                                                                                                                           |
| `KYBERION_CLAUDE_NATIVE_SUBAGENT`                  | boolean                | no       | Set to 1 to delegate through Claude's provider-native sub-agents (one shared stream-json CLI session / SDK `agents`) instead of spawning `claude -p` per task. Fails closed with subagent_unavailable when a delegation cannot be observed. |
| `KYBERION_DELEGATION_SUMMARY_RETRY`                | boolean                | no       | Enables the continuation retry when a delegated final summary is too brief; set false, 0, or off only for controlled compatibility runs.                                                                                                    |
| `KYBERION_DISABLE_AGENT_RUNTIME_SUPERVISOR_DAEMON` | boolean                | no       | Disable the agent runtime supervisor daemon for controlled local or test runs.                                                                                                                                                              |
| `KYBERION_DISABLE_EMBEDDINGS`                      | boolean                | no       | Disable embedding generation and use the non-semantic retrieval path.                                                                                                                                                                       |
| `KYBERION_ENABLE_SERVICE_ACTUATE_TOOL`             | boolean                | no       | Enable the service-actuate tool for trusted operators after capability approval.                                                                                                                                                            |
| `KYBERION_ENTITY_GOVERNANCE_STRICT_WARNINGS`       | boolean                | no       | Fail check:entity-governance when observed warnings exist.                                                                                                                                                                                  |
| `KYBERION_ENV_REGISTRY_STRICT`                     | boolean                | no       | Strict startup validation of KYBERION_* variables; on by default. Set to 0/false to downgrade unregistered or mistyped variables to warnings. Register new names in env-registry.json via pnpm generate:env-registry.                       |
| `KYBERION_ENV_REGISTRY_STRICT_DOCS`                | boolean                | no       | Generator-only strictness: when enabled, the env-registry drift check also fails while any registry entry remains undocumented. Off by default; independent of KYBERION_ENV_REGISTRY_STRICT (CLI startup validation).                       |
| `KYBERION_SECRET_ENCRYPTION`                       | enum: none \| keychain | no       | At-rest encryption mode for connection documents (AC-05). none=plaintext (default, compatible); keychain=AES-256-GCM with a macOS-keychain-held key. Migrate with pnpm secrets:encrypt (--decrypt exports back to plaintext).               |
| `KYBERION_SKIP_TYPE_RATCHET`                       | boolean                | no       | Skip type-ratchet validation for controlled image or context build runs; CI still enforces the ratchet.                                                                                                                                     |
| `KYBERION_SPEND_GUARD_TEST`                        | boolean                | no       | Test-only opt-in: lets vitest exercise the reasoning-path spend guard against real history/policy (normally disabled under VITEST).                                                                                                         |
| `KYBERION_TRUST_PROXY`                             | boolean                | no       | Trust x-forwarded-for and x-real-ip as the client peer for local-surface detection. Keep disabled unless a trusted reverse proxy overwrites these headers.                                                                                  |

## tuning (43)

| Variable                                       | Type   | Required | Description                                                                                                                                                             |
| ---------------------------------------------- | ------ | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `KYBERION_AGENT_BOOT_READY_TIMEOUT_MS`         | number | no       | Milliseconds an agent adapter waits for a newly started runtime to become ready before failing the boot request.                                                        |
| `KYBERION_AGENT_IDLE_TIMEOUT_MS`               | number | no       | Milliseconds an inactive agent runtime may remain before lifecycle cleanup; the default is 20 minutes.                                                                  |
| `KYBERION_AGENT_INFLIGHT_LIMIT`                | number | no       | Maximum concurrent requests admitted for one agent runtime; excess work receives a bounded busy response.                                                               |
| `KYBERION_AGENT_RING`                          | number | no       | Numeric policy ring supplied to operation-policy evaluation; higher rings may have stricter execution rules.                                                            |
| `KYBERION_AGENT_RUNTIME_SUPERVISOR_PORT`       | number | no       | TCP port for the agent runtime supervisor when transport is tcp; zero permits the operating system to select a port.                                                    |
| `KYBERION_AGENT_TURN_TIMEOUT_MS`               | number | no       | Maximum milliseconds allowed for one agent conversation turn before the mediator returns a timeout.                                                                     |
| `KYBERION_AGY_CLI_TIMEOUT_MS`                  | number | no       | Optional numeric tuning value for agy cli timeout ms; leave unset to use the governed default.                                                                          |
| `KYBERION_AUDIT_FORWARDER_TIMEOUT_MS`          | number | no       | Optional numeric tuning value for audit forwarder timeout ms; leave unset to use the governed default.                                                                  |
| `KYBERION_CHRONOS_IDLE_TIMEOUT_MS`             | number | no       | Optional numeric tuning value for chronos idle timeout ms; leave unset to use the governed default.                                                                     |
| `KYBERION_CLAUDE_CLI_TIMEOUT_MS`               | number | no       | Optional numeric tuning value for claude cli timeout ms; leave unset to use the governed default.                                                                       |
| `KYBERION_CODEX_CLI_TIMEOUT_MS`                | number | no       | Timeout in milliseconds for a Codex CLI query; unset keeps the adapter default.                                                                                         |
| `KYBERION_CONTEXT_BUFFER_TOKENS`               | number | no       | Optional numeric tuning value for context buffer tokens; leave unset to use the governed default.                                                                       |
| `KYBERION_CONTEXT_RESERVE_TOKENS`              | number | no       | Optional numeric tuning value for context reserve tokens; leave unset to use the governed default.                                                                      |
| `KYBERION_CONTEXT_WINDOW_TOKENS`               | number | no       | Optional numeric tuning value for context window tokens; leave unset to use the governed default.                                                                       |
| `KYBERION_DELEGATION_DEPTH`                    | number | no       | Delegation hop depth propagated to child processes for policy limits; operator-started roots default to zero.                                                           |
| `KYBERION_DELEGATION_KILL_GRACE_MS`            | number | no       | Milliseconds between SIGTERM and SIGKILL when a delegated child exceeds its wall-clock budget; the default is 5 seconds.                                                |
| `KYBERION_DELEGATION_MAX_CONCURRENCY`          | number | no       | Global maximum number of concurrent delegated tasks admitted by the process semaphore; the default is 4.                                                                |
| `KYBERION_DELEGATION_PROVIDER_MAX_CONCURRENCY` | number | no       | Default maximum concurrent delegated tasks per provider when no provider-specific cap is configured; the default is 2.                                                  |
| `KYBERION_DELEGATION_WALL_CLOCK_MS`            | number | no       | Wall-clock budget per delegated task before timeout and child termination; the default is 10 minutes.                                                                   |
| `KYBERION_DEPLOY_TIMEOUT_MS`                   | number | no       | Optional numeric tuning value for deploy timeout ms; leave unset to use the governed default.                                                                           |
| `KYBERION_FLUID_AUDIO_STT_TIMEOUT_MS`          | number | no       | Optional timeout in milliseconds for the FluidAudio/Parakeet batch STT bridge; unset uses the shell bridge default of five minutes.                                     |
| `KYBERION_GEMINI_CLI_TIMEOUT`                  | number | no       | Optional numeric tuning value for gemini cli timeout; leave unset to use the governed default.                                                                          |
| `KYBERION_GENERATION_SCHEDULE_INTERVAL_MS`     | number | no       | Optional numeric tuning value for generation schedule interval ms; leave unset to use the governed default.                                                             |
| `KYBERION_GLOBAL_INFLIGHT_LIMIT`               | number | no       | Optional numeric tuning value for global inflight limit; leave unset to use the governed default.                                                                       |
| `KYBERION_GROK_CLI_TIMEOUT_MS`                 | number | no       | Optional numeric tuning value for grok cli timeout ms; leave unset to use the governed default.                                                                         |
| `KYBERION_KILL_SWITCH_INTERVAL_MS`             | number | no       | Optional numeric tuning value for kill switch interval ms; leave unset to use the governed default.                                                                     |
| `KYBERION_MEDIA_BACKEND_PROBE_TTL_MS`          | number | no       | Optional numeric tuning value for media backend probe ttl ms; leave unset to use the governed default.                                                                  |
| `KYBERION_MFLUX_TIMEOUT_MS`                    | number | no       | Maximum local mflux generation time in milliseconds; defaults to 30 minutes.                                                                                            |
| `KYBERION_OAUTH_CALLBACK_PORT`                 | number | no       | Local TCP port for the OAuth callback surface; the default is 8787 and remote binding still requires the callback host and deployment security controls.                |
| `KYBERION_PEER_PORT`                           | number | no       | Optional numeric tuning value for peer port; leave unset to use the governed default.                                                                                   |
| `KYBERION_PROVIDER_DEMOTION_TTL_MS`            | number | no       | Optional numeric tuning value for provider demotion ttl ms; leave unset to use the governed default.                                                                    |
| `KYBERION_PTY_IDLE_TIMEOUT_MS`                 | number | no       | Optional numeric tuning value for pty idle timeout ms; leave unset to use the governed default.                                                                         |
| `KYBERION_REASONING_IN_PLACE_RETRIES`          | number | no       | Optional numeric tuning value for reasoning in place retries; leave unset to use the governed default.                                                                  |
| `KYBERION_REASONING_RETRY_BASE_MS`             | number | no       | Optional numeric tuning value for reasoning retry base ms; leave unset to use the governed default.                                                                     |
| `KYBERION_RUNTIME_SWEEP_INTERVAL_MS`           | number | no       | Optional numeric tuning value for runtime sweep interval ms; leave unset to use the governed default.                                                                   |
| `KYBERION_SECRET_RESOLVER_TIMEOUT_MS`          | number | no       | Timeout in milliseconds for the external secret resolver command.                                                                                                       |
| `KYBERION_SMTP_PORT`                           | number | no       | TCP port used by the email actuator SMTP backend; use the port required by the approved mail service, commonly 465 or 587.                                              |
| `KYBERION_STT_PRIORITY`                        | number | no       | Numeric priority assigned to the explicit shell STT bridge when multiple speech-to-text bridges are registered; higher-level selection may use this value for ordering. |
| `KYBERION_STT_TIMEOUT_MS`                      | number | no       | Optional timeout in milliseconds for the explicit shell batch STT bridge; unset uses the bridge default of five minutes.                                                |
| `KYBERION_STT_WINDOW_SEC`                      | number | no       | Audio window length in seconds for streaming Whisper adapters; defaults are adapter-specific.                                                                           |
| `KYBERION_TERMINAL_DISCONNECT_TIMEOUT_MS`      | number | no       | Optional numeric tuning value for terminal disconnect timeout ms; leave unset to use the governed default.                                                              |
| `KYBERION_TERMINAL_SESSION_RETENTION_MS`       | number | no       | Optional numeric tuning value for terminal session retention ms; leave unset to use the governed default.                                                               |
| `KYBERION_WORKITEM_RESPONSE_TIMEOUT_MS`        | number | no       | Maximum milliseconds a mission work item waits for one provider response before recording a blocked result.                                                             |

## provider (79)

| Variable                                  | Type                                         | Required | Description                                                                                                                                                                                                                                                                                                                                                                                                 |
| ----------------------------------------- | -------------------------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `KYBERION_A2UI_BRIDGE_URL`                | string                                       | no       | Optional A2UI rendering bridge endpoint used by the presence actuator; keep it on an approved local or trusted surface and do not treat it as an authorization boundary.                                                                                                                                                                                                                                    |
| `KYBERION_AGENT_RUNTIME_SUPERVISOR_HOST`  | string                                       | no       | Bind host for the agent runtime supervisor daemon; the default is loopback so the local control endpoint is not exposed remotely by omission.                                                                                                                                                                                                                                                               |
| `KYBERION_AGY_AGENT`                      | string                                       | no       | Generated AGY workspace agent name selected for CLI sessions, for example kyberion-reviewer.                                                                                                                                                                                                                                                                                                                |
| `KYBERION_AGY_CLI_MODEL`                  | string                                       | no       | Model name passed to the AGY CLI backend.                                                                                                                                                                                                                                                                                                                                                                   |
| `KYBERION_ANTHROPIC_FAST_MODEL`           | string                                       | no       | Fast model override for the Anthropic API route.                                                                                                                                                                                                                                                                                                                                                            |
| `KYBERION_ANTHROPIC_MODEL`                | string                                       | no       | Model override for the Anthropic API route.                                                                                                                                                                                                                                                                                                                                                                 |
| `KYBERION_AUDIO_RECORD_COMMAND`           | string                                       | no       | Local microphone capture command used by the voice sample recorder; it receives governed template values such as output and duration and is not a general-purpose user command entrypoint.                                                                                                                                                                                                                  |
| `KYBERION_AUDIT_FORWARDER_COMMAND`        | string                                       | no       | Optional shell sink for forwarding audit events after the governed audit sink records them locally; use an explicitly approved command.                                                                                                                                                                                                                                                                     |
| `KYBERION_AUDIT_FORWARDER_URL`            | string                                       | no       | Optional HTTP destination for forwarding audit events; outbound delivery remains subject to the egress policy and local audit persistence is authoritative.                                                                                                                                                                                                                                                 |
| `KYBERION_BLUEBUBBLES_URL`                | string                                       | no       | Base URL of the local or approved BlueBubbles bridge used for iMessage operations; it configures transport only and does not grant tenant access.                                                                                                                                                                                                                                                           |
| `KYBERION_CLAUDE_CLI_MODEL`               | string                                       | no       | Model name passed to the Claude CLI backend.                                                                                                                                                                                                                                                                                                                                                                |
| `KYBERION_CLAUDE_FAST_MODEL`              | string                                       | no       | Compatibility-alias fast model override for Claude routes.                                                                                                                                                                                                                                                                                                                                                  |
| `KYBERION_CLAUDE_MODEL`                   | string                                       | no       | Compatibility-alias model override for Claude routes.                                                                                                                                                                                                                                                                                                                                                       |
| `KYBERION_CODEX_CLI_MODEL`                | string                                       | no       | Model name passed to the Codex CLI backend.                                                                                                                                                                                                                                                                                                                                                                 |
| `KYBERION_CODEX_MODEL`                    | string                                       | no       | Model identifier used by the Codex agent and intent compiler routes.                                                                                                                                                                                                                                                                                                                                        |
| `KYBERION_CODEX_MODEL_PROVIDER`           | string                                       | no       | Provider hint paired with the Codex model identifier for app-server execution.                                                                                                                                                                                                                                                                                                                              |
| `KYBERION_COMFY_BASE_URL`                 | string                                       | no       | Base URL for the ComfyUI image or video generation provider; requests must remain within the governed provider and egress boundaries.                                                                                                                                                                                                                                                                       |
| `KYBERION_COPILOT_MODEL`                  | string                                       | no       | Model name requested from the Copilot ACP backend.                                                                                                                                                                                                                                                                                                                                                          |
| `KYBERION_DEPLOY_COMMAND`                 | string                                       | no       | Optional shell deployment adapter command for CI/CD dispatch; real deployment remains behind the deployment approval gate and explicit configuration.                                                                                                                                                                                                                                                       |
| `KYBERION_FLUID_AUDIO_STT_COMMAND`        | string                                       | no       | Local FluidAudio/Parakeet JSON bridge command for speech-to-text; the bridge receives a governed audio path and returns structured transcription output.                                                                                                                                                                                                                                                    |
| `KYBERION_GEMINI_CLI_MODEL`               | string                                       | no       | Model name passed to the Gemini CLI backend.                                                                                                                                                                                                                                                                                                                                                                |
| `KYBERION_GEMINI_FAST_MODEL`              | string                                       | no       | Fast model override for the Gemini route.                                                                                                                                                                                                                                                                                                                                                                   |
| `KYBERION_GEMINI_MODEL`                   | string                                       | no       | Model identifier used by the Gemini API and routing fallback.                                                                                                                                                                                                                                                                                                                                               |
| `KYBERION_GEMINI_URL`                     | string                                       | no       | Optional Gemini API base URL override; leave unset for the provider default and use only an endpoint permitted by the egress policy.                                                                                                                                                                                                                                                                        |
| `KYBERION_GEMINI_VIDEO_URL`               | string                                       | no       | Optional base URL override for the Google Veo video-generation API.                                                                                                                                                                                                                                                                                                                                         |
| `KYBERION_GROK_API_MODEL`                 | string                                       | no       | xAI Grok API model id. Defaults to grok-4.6 when omitted.                                                                                                                                                                                                                                                                                                                                                   |
| `KYBERION_GROK_API_URL`                   | string                                       | no       | Optional xAI-compatible API base URL; defaults to https://api.x.ai/v1.                                                                                                                                                                                                                                                                                                                                      |
| `KYBERION_GROK_CLI_MODEL`                 | string                                       | no       | Model name passed to the Grok Build CLI backend.                                                                                                                                                                                                                                                                                                                                                            |
| `KYBERION_INTENT_COMPILER_MODEL`          | string                                       | no       | Explicit model override for intent contract compilation.                                                                                                                                                                                                                                                                                                                                                    |
| `KYBERION_INTENT_COMPILER_MODEL_PROVIDER` | string                                       | no       | Provider hint paired with the intent compiler model override.                                                                                                                                                                                                                                                                                                                                               |
| `KYBERION_INTENT_COMPILER_PROVIDER`       | string                                       | no       | Provider route selected for intent contract compilation.                                                                                                                                                                                                                                                                                                                                                    |
| `KYBERION_LLAMACPP_MODEL`                 | string                                       | no       | Model identifier sent to the llama.cpp OpenAI-compatible endpoint.                                                                                                                                                                                                                                                                                                                                          |
| `KYBERION_LLAMACPP_URL`                   | string                                       | no       | Base URL for the llama.cpp OpenAI-compatible endpoint.                                                                                                                                                                                                                                                                                                                                                      |
| `KYBERION_LM_STUDIO_MODEL`                | string                                       | no       | Compatibility-alias model identifier sent to the LM Studio endpoint.                                                                                                                                                                                                                                                                                                                                        |
| `KYBERION_LM_STUDIO_URL`                  | string                                       | no       | Compatibility-alias base URL for the LM Studio OpenAI-compatible endpoint.                                                                                                                                                                                                                                                                                                                                  |
| `KYBERION_LMSTUDIO_MODEL`                 | string                                       | no       | Model identifier sent to the LM Studio OpenAI-compatible endpoint.                                                                                                                                                                                                                                                                                                                                          |
| `KYBERION_LMSTUDIO_URL`                   | string                                       | no       | Base URL for the LM Studio OpenAI-compatible endpoint.                                                                                                                                                                                                                                                                                                                                                      |
| `KYBERION_LOCAL_LLM_MODEL`                | string                                       | no       | Model id for the local LLM endpoint.                                                                                                                                                                                                                                                                                                                                                                        |
| `KYBERION_LOCAL_LLM_URL`                  | string                                       | no       | Base URL of a local OpenAI-compatible LLM endpoint.                                                                                                                                                                                                                                                                                                                                                         |
| `KYBERION_LOCALAI_MODEL`                  | string                                       | no       | Model identifier sent to the LocalAI OpenAI-compatible endpoint.                                                                                                                                                                                                                                                                                                                                            |
| `KYBERION_LOCALAI_URL`                    | string                                       | no       | Base URL for the LocalAI OpenAI-compatible endpoint.                                                                                                                                                                                                                                                                                                                                                        |
| `KYBERION_MFLUX_MODEL`                    | string                                       | no       | Model identifier passed to the local mflux image-generation backend; it selects the model and does not configure a remote provider endpoint.                                                                                                                                                                                                                                                                |
| `KYBERION_MINIMAX_VIDEO_URL`              | string                                       | no       | Optional base URL override for the MiniMax Hailuo video-generation API.                                                                                                                                                                                                                                                                                                                                     |
| `KYBERION_MLX_EMBED_MODEL`                | string                                       | no       | Hugging Face model identifier used by the MLX embedding backend.                                                                                                                                                                                                                                                                                                                                            |
| `KYBERION_MLX_MODEL`                      | string                                       | no       | Model identifier sent to the MLX-LM OpenAI-compatible endpoint.                                                                                                                                                                                                                                                                                                                                             |
| `KYBERION_MLX_URL`                        | string                                       | no       | Base URL for the MLX-LM OpenAI-compatible endpoint.                                                                                                                                                                                                                                                                                                                                                         |
| `KYBERION_MLX_WHISPER_MODEL`              | string                                       | no       | Model identifier used by the managed MLX Whisper streaming adapter.                                                                                                                                                                                                                                                                                                                                         |
| `KYBERION_NEMOTRON_MODEL`                 | string                                       | no       | Model identifier sent to the Nemotron OpenAI-compatible endpoint.                                                                                                                                                                                                                                                                                                                                           |
| `KYBERION_NEMOTRON_URL`                   | string                                       | no       | Base URL for the Nemotron OpenAI-compatible endpoint.                                                                                                                                                                                                                                                                                                                                                       |
| `KYBERION_OAUTH_CALLBACK_HOST`            | string                                       | no       | Bind host for the local OAuth callback surface; the default is loopback and a remote host should be used only with an explicitly secured deployment.                                                                                                                                                                                                                                                        |
| `KYBERION_OLLAMA_MODEL`                   | string                                       | no       | Model name sent to the Ollama OpenAI-compatible endpoint.                                                                                                                                                                                                                                                                                                                                                   |
| `KYBERION_OLLAMA_URL`                     | string                                       | no       | Base URL for the Ollama OpenAI-compatible endpoint.                                                                                                                                                                                                                                                                                                                                                         |
| `KYBERION_OPENAI_MODEL`                   | string                                       | no       | Model identifier used as the OpenAI vision route fallback.                                                                                                                                                                                                                                                                                                                                                  |
| `KYBERION_OPENAI_VIDEO_URL`               | string                                       | no       | Optional base URL override for the OpenAI Videos API.                                                                                                                                                                                                                                                                                                                                                       |
| `KYBERION_OPENAI_VISION_MODEL`            | string                                       | no       | Model identifier used for OpenAI-compatible vision requests.                                                                                                                                                                                                                                                                                                                                                |
| `KYBERION_OPENROUTER_COST_POLICY`         | enum: free-only \| paid-allowed              | no       | OpenRouter cost guard. `free-only` is the safe default; `paid-allowed` explicitly opts into paid inference.                                                                                                                                                                                                                                                                                                 |
| `KYBERION_OPENROUTER_MODEL`               | string                                       | no       | OpenRouter model id sent to the chat completions endpoint. Defaults to the free model router when omitted.                                                                                                                                                                                                                                                                                                  |
| `KYBERION_OPENROUTER_PROFILE`             | enum: free-router \| free-pinned \| explicit | no       | OpenRouter model selection profile: free-router, pinned :free model, or explicit model.                                                                                                                                                                                                                                                                                                                     |
| `KYBERION_OPENROUTER_REQUIRED_PARAMETERS` | string                                       | no       | Comma-separated OpenRouter supported parameters required by the selected model.                                                                                                                                                                                                                                                                                                                             |
| `KYBERION_OPENROUTER_URL`                 | string                                       | no       | Optional OpenRouter-compatible API base URL; defaults to https://openrouter.ai/api/v1.                                                                                                                                                                                                                                                                                                                      |
| `KYBERION_OPS_ALERT_WEBHOOK_URL`          | string                                       | no       | Optional webhook destination for operational alerts; delivery is attempted only when configured and remains subject to egress policy, while the local alert ledger remains the source of record.                                                                                                                                                                                                            |
| `KYBERION_OS_GUARDED_SURFACE_URL`         | string                                       | no       | URL of the guarded OS decision surface shown to a human when an operating-system action is held; it is a presentation link, not an authorization input.                                                                                                                                                                                                                                                     |
| `KYBERION_PEER_HOST`                      | string                                       | no       | Bind host for the peer messaging and peer conversation servers; the default is loopback and remote binding requires the mesh transport security configuration.                                                                                                                                                                                                                                              |
| `KYBERION_REASONING_BACKEND`              | string                                       | no       | Reasoning backend selector. Common values: claude-cli (local claude CLI, no API key), anthropic (ANTHROPIC_API_KEY), codex-cli, agy-cli (Antigravity CLI), grok-cli (Grok Build CLI), grok-api (XAI_API_KEY or KYBERION_GROK_API_KEY), openrouter (OPENROUTER_API_KEY or KYBERION_OPENROUTER_KEY), stub (offline/deterministic tests). gemini-cli remains available for explicit Enterprise configurations. |
| `KYBERION_REASONING_MODEL`                | string                                       | no       | Global fallback model override for reasoning routes.                                                                                                                                                                                                                                                                                                                                                        |
| `KYBERION_RUNWAY_VIDEO_URL`               | string                                       | no       | Optional base URL override for the Runway video-generation API.                                                                                                                                                                                                                                                                                                                                             |
| `KYBERION_SECRET_RESOLVER_COMMAND`        | string                                       | no       | Command used by the secret resolver to obtain named secrets.                                                                                                                                                                                                                                                                                                                                                |
| `KYBERION_SILERO_VAD_MODEL`               | string                                       | no       | Filesystem path to the Silero VAD ONNX model used by the local realtime voice bridge; when unset, the voice loop uses its configured fallback VAD path.                                                                                                                                                                                                                                                     |
| `KYBERION_SMTP_HOST`                      | string                                       | no       | SMTP host that activates the email actuator SMTP mode; pair it with the separately managed SMTP credentials and keep outbound delivery within the approved egress boundary.                                                                                                                                                                                                                                 |
| `KYBERION_STT_COMMAND`                    | string                                       | no       | Speech-to-text bridge command override.                                                                                                                                                                                                                                                                                                                                                                     |
| `KYBERION_STT_MODEL`                      | string                                       | no       | Model identifier or size used by faster-whisper when STT_MODEL_DIR is not set.                                                                                                                                                                                                                                                                                                                              |
| `KYBERION_TERMINAL_HOST`                  | string                                       | no       | Bind host for the terminal bridge; the default is loopback so terminal control is not exposed remotely by default.                                                                                                                                                                                                                                                                                          |
| `KYBERION_TTS_COMMAND`                    | string                                       | no       | Optional streaming text-to-speech executable for realtime voice and meeting flows; it reads newline-delimited UTF-8 text and emits PCM audio through the governed bridge.                                                                                                                                                                                                                                   |
| `KYBERION_TTS_PLAY_COMMAND`               | string                                       | no       | Optional PCM playback command used by the streaming TTS bridge; it overrides the platform default player and remains a local runtime configuration.                                                                                                                                                                                                                                                         |
| `KYBERION_VLLM_MODEL`                     | string                                       | no       | Model identifier sent to the vLLM OpenAI-compatible endpoint.                                                                                                                                                                                                                                                                                                                                               |
| `KYBERION_VLLM_URL`                       | string                                       | no       | Base URL for the vLLM OpenAI-compatible endpoint.                                                                                                                                                                                                                                                                                                                                                           |
| `KYBERION_WINDOWS_AI_ENDPOINT`            | string                                       | no       | Foundry Local or other approved Windows local AI endpoint used by the Windows assistance bridge; keep the endpoint local or explicitly allowlisted.                                                                                                                                                                                                                                                         |
| `KYBERION_WINDOWS_AI_MODEL`               | string                                       | no       | Model identifier selected by the Windows local AI bridge; it is paired with KYBERION_WINDOWS_AI_ENDPOINT and does not itself grant external access.                                                                                                                                                                                                                                                         |
| `KYBERION_WINDOWS_STT_BACKEND`            | string                                       | no       | Selects the Windows speech-to-text backend, such as faster_whisper, for the local voice actuator; the selected backend must be installed and available in the governed runtime.                                                                                                                                                                                                                             |

## runtime (148)

| Variable                                      | Type                               | Required | Description                                                                                                                                                                     |
| --------------------------------------------- | ---------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `KYBERION_A2A_SIGNATURE`                      | enum: warn \| enforce              | no       | A2A envelope signature policy: warn audits missing signatures while enforce rejects unsigned or unknown-sender messages.                                                        |
| `KYBERION_AGENT_ID`                           | string                             | no       | Logical agent identity attached to runtime, task, and audit records; use a stable non-secret identifier.                                                                        |
| `KYBERION_AGENT_RUNTIME_SUPERVISOR_TRANSPORT` | enum: unix \| tcp                  | no       | Transport used by the agent runtime supervisor; unix is the local default, while tcp requires an explicitly controlled host and port.                                           |
| `KYBERION_AGENT_TIER`                         | string                             | no       | Execution authority tier propagated to secure-io and policy checks; child processes may downgrade but must not use it to bypass governed access.                                |
| `KYBERION_AGY_SANDBOX`                        | string                             | no       | Optional runtime setting for agy sandbox; leave unset to use the built-in configuration.                                                                                        |
| `KYBERION_AGY_SDK_CWD`                        | string                             | no       | Working directory passed from the AGY SDK adapter to its Python bridge.                                                                                                         |
| `KYBERION_ANTHROPIC_NATIVE_DEFERRED_TOOLS`    | string                             | no       | Optional runtime setting for anthropic native deferred tools; leave unset to use the built-in configuration.                                                                    |
| `KYBERION_APPLE_FM`                           | string                             | no       | Optional runtime setting for apple fm; leave unset to use the built-in configuration.                                                                                           |
| `KYBERION_APPLE_FM_SAMPLE_AUDIO`              | string                             | no       | Optional runtime setting for apple fm sample audio; leave unset to use the built-in configuration.                                                                              |
| `KYBERION_AUDIO_BUS`                          | string                             | no       | Optional runtime setting for audio bus; leave unset to use the built-in configuration.                                                                                          |
| `KYBERION_AUDIO_DEVICE_UID`                   | string                             | no       | Optional runtime setting for audio device uid; leave unset to use the built-in configuration.                                                                                   |
| `KYBERION_AUDIO_PERMISSION_CONFIRMED`         | string                             | no       | Operator acknowledgement that the meeting preflight microphone permission was confirmed for this session; use 1 only after the OS permission check succeeds.                    |
| `KYBERION_AUDIT_CONTINUITY_ENFORCE`           | string                             | no       | Optional runtime setting for audit continuity enforce; leave unset to use the built-in configuration.                                                                           |
| `KYBERION_AUDIT_FORWARDER_HEADERS`            | string                             | no       | Optional runtime setting for audit forwarder headers; leave unset to use the built-in configuration.                                                                            |
| `KYBERION_AUTO_SEAL`                          | boolean                            | no       | Automatically seal completed personal-tier missions.                                                                                                                            |
| `KYBERION_BEST_OF_N`                          | string                             | no       | Optional runtime setting for best of n; leave unset to use the built-in configuration.                                                                                          |
| `KYBERION_BEST_OF_PROVIDERS_LIVE`             | string                             | no       | Optional runtime setting for best of providers live; leave unset to use the built-in configuration.                                                                             |
| `KYBERION_BLACKHOLE_DEVICE_LABEL`             | string                             | no       | Optional runtime setting for blackhole device label; leave unset to use the built-in configuration.                                                                             |
| `KYBERION_BLACKHOLE_INPUT_UID`                | string                             | no       | Optional runtime setting for blackhole input uid; leave unset to use the built-in configuration.                                                                                |
| `KYBERION_BLACKHOLE_OUTPUT_UID`               | string                             | no       | Optional runtime setting for blackhole output uid; leave unset to use the built-in configuration.                                                                               |
| `KYBERION_BLACKHOLE_SCRIPT`                   | string                             | no       | Optional runtime setting for blackhole script; leave unset to use the built-in configuration.                                                                                   |
| `KYBERION_BLUEBUBBLES_SEND_METHOD`            | string                             | no       | Optional runtime setting for bluebubbles send method; leave unset to use the built-in configuration.                                                                            |
| `KYBERION_BRAND_FONT_STACK`                   | string                             | no       | Optional runtime setting for brand font stack; leave unset to use the built-in configuration.                                                                                   |
| `KYBERION_BRAND_MONO_STACK`                   | string                             | no       | Optional runtime setting for brand mono stack; leave unset to use the built-in configuration.                                                                                   |
| `KYBERION_CLAUDE_AGENT_TOOLS`                 | string                             | no       | Optional runtime setting for claude agent tools; leave unset to use the built-in configuration.                                                                                 |
| `KYBERION_CLAUDE_CLI_EXTRA_ARGS`              | string                             | no       | Optional runtime setting for claude cli extra args; leave unset to use the built-in configuration.                                                                              |
| `KYBERION_CODEX_APPROVAL`                     | enum: strict \| relaxed            | no       | Approval mode for the Codex app-server adapter; strict keeps the governed approval path and relaxed permits provider-side relaxed approvals.                                    |
| `KYBERION_CODEX_CLI_EXTRA_ARGS`               | string                             | no       | Whitespace-separated extra arguments appended to Codex CLI query invocations.                                                                                                   |
| `KYBERION_CODEX_INSTANCES`                    | string                             | no       | Comma-separated Codex instance identifiers used by provider-health routing to track independent quotas and demotions.                                                           |
| `KYBERION_CODEX_MODE`                         | enum: app-server \| exec \| legacy | no       | Codex transport mode; app-server uses the JSON-RPC adapter, while exec and legacy select the CLI adapter.                                                                       |
| `KYBERION_COMPUTER_SURFACE_PRINCIPAL`         | string                             | no       | Optional runtime setting for computer surface principal; leave unset to use the built-in configuration.                                                                         |
| `KYBERION_CONTEXT_PACK_ID`                    | string                             | no       | Optional runtime setting for context pack id; leave unset to use the built-in configuration.                                                                                    |
| `KYBERION_CURATION_SCAN_ROOTS`                | string                             | no       | Optional runtime setting for curation scan roots; leave unset to use the built-in configuration.                                                                                |
| `KYBERION_CURATION_TENANT_ROOTDIR`            | string                             | no       | Test and controlled-operation root override for tenant curation ledger discovery; it is resolved through the governed root resolver and must not be used to widen tenant scope. |
| `KYBERION_CUSTOMER`                           | string                             | no       | Active customer profile slug used to resolve the profile root.                                                                                                                  |
| `KYBERION_DELEGATION_PROVIDER_CAPS`           | string                             | no       | Optional JSON object mapping provider ids to positive concurrency caps, for example {"claude":3}; invalid JSON is ignored.                                                      |
| `KYBERION_DEPLOY_PROJECT`                     | string                             | no       | Optional runtime setting for deploy project; leave unset to use the built-in configuration.                                                                                     |
| `KYBERION_DEPLOYMENT_PROJECT`                 | string                             | no       | Optional runtime setting for deployment project; leave unset to use the built-in configuration.                                                                                 |
| `KYBERION_DISPATCH_MAX_ROUNDS`                | string                             | no       | Optional runtime setting for dispatch max rounds; leave unset to use the built-in configuration.                                                                                |
| `KYBERION_DRAFT_REFINE`                       | string                             | no       | Optional runtime setting for draft refine; leave unset to use the built-in configuration.                                                                                       |
| `KYBERION_EGRESS_POLICY`                      | string                             | no       | Network egress policy override; use warn to observe or enforce to reject destinations outside the governed allowlist. The policy file remains authoritative for domains.        |
| `KYBERION_EMAIL_FROM`                         | string                             | no       | Optional runtime setting for email from; leave unset to use the built-in configuration.                                                                                         |
| `KYBERION_ENTITY_GOVERNANCE`                  | string                             | no       | Optional runtime setting for entity governance; leave unset to use the built-in configuration.                                                                                  |
| `KYBERION_FIRST_WIN_LIVE`                     | string                             | no       | Optional runtime setting for first win live; leave unset to use the built-in configuration.                                                                                     |
| `KYBERION_FORMAT`                             | string                             | no       | Optional runtime setting for format; leave unset to use the built-in configuration.                                                                                             |
| `KYBERION_GATE_OVERRIDE_SIGNATURE`            | enum: warn \| enforce              | no       | Human-override signature policy: warn observes missing or invalid gate signatures while enforce rejects them.                                                                   |
| `KYBERION_GEMINI_TTS_VOICE`                   | string                             | no       | Optional runtime setting for gemini tts voice; leave unset to use the built-in configuration.                                                                                   |
| `KYBERION_GOAL_LOOP_MAX_ROUNDS`               | string                             | no       | Optional runtime setting for goal loop max rounds; leave unset to use the built-in configuration.                                                                               |
| `KYBERION_GROK_CLI_EXTRA_ARGS`                | string                             | no       | Optional runtime setting for grok cli extra args; leave unset to use the built-in configuration.                                                                                |
| `KYBERION_HARNESS_SUBAGENT`                   | string                             | no       | Optional runtime setting for harness subagent; leave unset to use the built-in configuration.                                                                                   |
| `KYBERION_HISTORY_SEARCH_DB`                  | string                             | no       | Optional runtime setting for history search db; leave unset to use the built-in configuration.                                                                                  |
| `KYBERION_HOST_AGENT_ACTIVE`                  | string                             | no       | Optional runtime setting for host agent active; leave unset to use the built-in configuration.                                                                                  |
| `KYBERION_IMESSAGE_WAKE_WORD`                 | string                             | no       | Optional runtime setting for imessage wake word; leave unset to use the built-in configuration.                                                                                 |
| `KYBERION_IN_SESSION_SUBAGENT`                | string                             | no       | Optional runtime setting for in session subagent; leave unset to use the built-in configuration.                                                                                |
| `KYBERION_INGEST_QUOTA_TEST`                  | string                             | no       | Optional runtime setting for ingest quota test; leave unset to use the built-in configuration.                                                                                  |
| `KYBERION_INJECTION_SCOPE`                    | string                             | no       | Optional runtime setting for injection scope; leave unset to use the built-in configuration.                                                                                    |
| `KYBERION_INJECTION_SUSPECTED`                | string                             | no       | Optional runtime setting for injection suspected; leave unset to use the built-in configuration.                                                                                |
| `KYBERION_KI_CACHE_MAX_MB`                    | string                             | no       | Optional runtime setting for ki cache max mb; leave unset to use the built-in configuration.                                                                                    |
| `KYBERION_KNOWLEDGE_REFS`                     | string                             | no       | Comma-separated knowledge references attached to ambient reasoning visibility for the active mission context.                                                                   |
| `KYBERION_KNOWLEDGE_TIER`                     | string                             | no       | Knowledge tier hint propagated into pipeline facet resolution; accepted values are public, confidential, and personal, with invalid values falling back to public.              |
| `KYBERION_LIVE_BLACKHOLE_TEST`                | string                             | no       | Optional runtime setting for live blackhole test; leave unset to use the built-in configuration.                                                                                |
| `KYBERION_LLM_CONCURRENCY`                    | string                             | no       | Optional runtime setting for llm concurrency; leave unset to use the built-in configuration.                                                                                    |
| `KYBERION_LOCALE`                             | string                             | no       | Optional runtime setting for locale; leave unset to use the built-in configuration.                                                                                             |
| `KYBERION_LOCALHOST_AUTOADMIN`                | string                             | no       | Allow loopback Chronos requests to use the compatibility localadmin principal when true or unset.                                                                               |
| `KYBERION_MANAGED_PYTHON_VERSION`             | string                             | no       | Optional runtime setting for managed python version; leave unset to use the built-in configuration.                                                                             |
| `KYBERION_MCP_CALLER_ROLE`                    | string                             | no       | Server-bound MCP caller role checked against mcp-tool-catalog.json; client arguments cannot elevate it.                                                                         |
| `KYBERION_MCP_NHI`                            | string                             | no       | Server-bound NHI used to bind MCP event and approval scope.                                                                                                                     |
| `KYBERION_MCP_PRINCIPAL`                      | string                             | no       | Server-bound principal name recorded for MCP request attribution.                                                                                                               |
| `KYBERION_MCP_TENANT`                         | string                             | no       | Server-bound tenant slug; a client-supplied tenant may only narrow this binding.                                                                                                |
| `KYBERION_MEMORY_AUTOPROMOTE`                 | string                             | no       | Optional runtime setting for memory autopromote; leave unset to use the built-in configuration.                                                                                 |
| `KYBERION_MESH_NAMESPACE`                     | string                             | no       | Optional runtime setting for mesh namespace; leave unset to use the built-in configuration.                                                                                     |
| `KYBERION_MESH_PEER_ID`                       | string                             | no       | Optional runtime setting for mesh peer id; leave unset to use the built-in configuration.                                                                                       |
| `KYBERION_MFLUX_PACKAGE`                      | string                             | no       | Package specification supplied to the local mflux runner; use this to select a pinned or locally available package build.                                                       |
| `KYBERION_MFLUX_QUANTIZE`                     | number                             | no       | Positive quantization level passed to the local mflux image-generation runner; defaults to 8.                                                                                   |
| `KYBERION_MFLUX_SEED`                         | string                             | no       | Optional deterministic seed passed to the local mflux image-generation runner.                                                                                                  |
| `KYBERION_MFLUX_STEPS`                        | number                             | no       | Positive diffusion step count for local mflux generation; the default is mode-dependent (4 for schnell and 20 for dev).                                                         |
| `KYBERION_MOS_PRINCIPAL`                      | string                             | no       | Optional runtime setting for mos principal; leave unset to use the built-in configuration.                                                                                      |
| `KYBERION_NHI_ACTOR`                          | string                             | no       | Non-human identity actor verification mode: off disables checks, warn audits violations, and enforce rejects unregistered, suspended, or retired actors; the default is warn.   |
| `KYBERION_NHI_ID`                             | string                             | no       | Optional runtime setting for nhi id; leave unset to use the built-in configuration.                                                                                             |
| `KYBERION_NON_INTERACTIVE`                    | string                             | no       | Optional runtime setting for non interactive; leave unset to use the built-in configuration.                                                                                    |
| `KYBERION_OAUTH_SERVICE_ID`                   | string                             | no       | Optional runtime setting for oauth service id; leave unset to use the built-in configuration.                                                                                   |
| `KYBERION_ONBOARDING_NON_INTERACTIVE_OK`      | string                             | no       | Optional runtime setting for onboarding non interactive ok; leave unset to use the built-in configuration.                                                                      |
| `KYBERION_ORGANIZATION_ID`                    | string                             | no       | Optional runtime setting for organization id; leave unset to use the built-in configuration.                                                                                    |
| `KYBERION_PEER_NETWORK_CATALOG`               | string                             | no       | Optional runtime setting for peer network catalog; leave unset to use the built-in configuration.                                                                               |
| `KYBERION_PERSONA`                            | string                             | no       | Active persona for the policy engine (set by withExecutionContext; rarely set manually).                                                                                        |
| `KYBERION_PHASE_GATE_MODE`                    | string                             | no       | Optional runtime setting for phase gate mode; leave unset to use the built-in configuration.                                                                                    |
| `KYBERION_PIPELINE_FALLBACK_ACTIVE`           | string                             | no       | Optional runtime setting for pipeline fallback active; leave unset to use the built-in configuration.                                                                           |
| `KYBERION_PREFIX`                             | string                             | no       | Optional runtime setting for prefix; leave unset to use the built-in configuration.                                                                                             |
| `KYBERION_PROJECT_ID`                         | string                             | no       | Optional runtime setting for project id; leave unset to use the built-in configuration.                                                                                         |
| `KYBERION_PROJECT_NAME`                       | string                             | no       | Optional runtime setting for project name; leave unset to use the built-in configuration.                                                                                       |
| `KYBERION_PROVIDER_CAPABILITY_PROBE`          | string                             | no       | Optional runtime setting for provider capability probe; leave unset to use the built-in configuration.                                                                          |
| `KYBERION_PROVIDER_CAPABILITY_REFRESH`        | string                             | no       | Force a provider capability re-probe and reasoning backend re-selection                                                                                                         |
| `KYBERION_PROVIDER_CAPABILITY_ROUTING`        | string                             | no       | Optional runtime setting for provider capability routing; leave unset to use the built-in configuration.                                                                        |
| `KYBERION_PROVIDER_DISCOVERY_REFRESH`         | string                             | no       | Optional runtime setting for provider discovery refresh; leave unset to use the built-in configuration.                                                                         |
| `KYBERION_PROVIDER_ENV_ALLOWLIST`             | string                             | no       | Provider child-process environment policy; allowlisting is enabled by default, while 0 is an explicit diagnostic escape hatch that passes through the parent environment.       |
| `KYBERION_PROVIDER_PRIORITY`                  | string                             | no       | Optional runtime setting for provider priority; leave unset to use the built-in configuration.                                                                                  |
| `KYBERION_PTC_CHILD`                          | string                             | no       | Optional runtime setting for ptc child; leave unset to use the built-in configuration.                                                                                          |
| `KYBERION_REASONING_ALLOW_SHELL_TOOL`         | string                             | no       | Explicitly enables the shell_exec tool for reasoning profiles that request it; only the literal true enables the capability and the shell policy still applies.                 |
| `KYBERION_REASONING_PROFILE`                  | string                             | no       | Optional runtime setting for reasoning profile; leave unset to use the built-in configuration.                                                                                  |
| `KYBERION_REMINDER_CC_AFTER_N`                | string                             | no       | Optional runtime setting for reminder cc after n; leave unset to use the built-in configuration.                                                                                |
| `KYBERION_RESTRICTED_ACTIONS_POLICY`          | string                             | no       | Optional runtime setting for restricted actions policy; leave unset to use the built-in configuration.                                                                          |
| `KYBERION_RESTRICTED_APPROVED_ITEMS`          | string                             | no       | Optional runtime setting for restricted approved items; leave unset to use the built-in configuration.                                                                          |
| `KYBERION_SECURITY_POSTURE`                   | string                             | no       | Security-screen posture override; accepted values are dangerous, auto, and strict. Invalid values are ignored and the schema-validated posture catalog is used.                 |
| `KYBERION_SECURITY_QUARANTINE_MAX_BYTES`      | string                             | no       | Optional runtime setting for security quarantine max bytes; leave unset to use the built-in configuration.                                                                      |
| `KYBERION_SILERO_VAD_PYTHON`                  | string                             | no       | Optional Python executable for the Silero VAD bridge; fallback order is the managed silero runtime, then KYBERION_PYTHON_BIN, then python3.                                     |
| `KYBERION_SMTP_USER`                          | string                             | no       | SMTP username used by the email actuator; keep account identity configuration aligned with the approved outbound mail service.                                                  |
| `KYBERION_SPEAKER_FAIRNESS_MUST_THRESHOLD`    | string                             | no       | Optional runtime setting for speaker fairness must threshold; leave unset to use the built-in configuration.                                                                    |
| `KYBERION_SPEAKER_FAIRNESS_TOTAL_THRESHOLD`   | string                             | no       | Optional runtime setting for speaker fairness total threshold; leave unset to use the built-in configuration.                                                                   |
| `KYBERION_STREAMING_STT_BRIDGE`               | string                             | no       | Streaming speech-to-text bridge id selected by the realtime loop; unset uses the deterministic stub, while installed shell or managed bridges may be selected explicitly.       |
| `KYBERION_STREAMING_TTS_BRIDGE`               | string                             | no       | Streaming text-to-speech bridge id selected by the realtime loop; unset uses the PCM stub and an installed shell or provider bridge can be selected explicitly.                 |
| `KYBERION_STT_ARGS`                           | string                             | no       | Optional comma-separated arguments appended to the shell streaming STT command; values are trimmed and empty items are ignored.                                                 |
| `KYBERION_STT_CAPABILITIES`                   | string                             | no       | Optional JSON object describing shell STT capabilities, such as timestamps, granularity, and local_only; invalid or unsafe shapes are ignored at the bridge boundary.           |
| `KYBERION_STT_COMPUTE_TYPE`                   | string                             | no       | Compute type passed to faster-whisper; CPU defaults to int8 and non-CPU devices default to float16.                                                                             |
| `KYBERION_STT_DEVICE`                         | string                             | no       | Device selected by the faster-whisper speech-to-text bridge, such as cpu or cuda.                                                                                               |
| `KYBERION_STT_LANGUAGE`                       | string                             | no       | Language hint for local Whisper speech-to-text adapters; managed streaming adapters default to ja when unset.                                                                   |
| `KYBERION_STT_OUTPUT_FORMAT`                  | enum: text \| json                 | no       | Output format expected from the explicit shell STT command; `json` enables structured text/capabilities/segments parsing, while text keeps stdout as the transcript.            |
| `KYBERION_STYLE_KEYS`                         | string                             | no       | Optional runtime setting for style keys; leave unset to use the built-in configuration.                                                                                         |
| `KYBERION_SUDO`                               | string                             | no       | Authority escalation switch consumed by the policy engine; use only for explicitly approved operations.                                                                         |
| `KYBERION_SUDO_SCOPE`                         | string                             | no       | Path scope restriction for KYBERION_SUDO escalation.                                                                                                                            |
| `KYBERION_SURFACE_ALLOWLISTS`                 | string                             | no       | JSON actor allowlist keyed by surface (for example {"telegram":["actor-id"]}); an invalid configured value fails closed for the affected surface.                               |
| `KYBERION_SURFACE_QUERY_PHASE`                | string                             | no       | Optional runtime setting for surface query phase; leave unset to use the built-in configuration.                                                                                |
| `KYBERION_SURFACE_QUERY_ROLE`                 | string                             | no       | Fallback role selector for surface-query provider resolution; a request or server-resolved context role takes precedence and this value does not grant authorization.           |
| `KYBERION_TASK_ID`                            | string                             | no       | Optional runtime setting for task id; leave unset to use the built-in configuration.                                                                                            |
| `KYBERION_TASK_MODEL_ROUTING`                 | string                             | no       | Task model-hint policy: advisory records routing hints without changing the manifest choice, while enforce applies a validated task model hint when present.                    |
| `KYBERION_TELEMETRY`                          | string                             | no       | Optional runtime setting for telemetry; leave unset to use the built-in configuration.                                                                                          |
| `KYBERION_TEN_VAD_PYTHON`                     | string                             | no       | Optional Python executable for the TEN VAD bridge; unset uses KYBERION_PYTHON_BIN when available and otherwise python3.                                                         |
| `KYBERION_TENANT`                             | string                             | no       | Active tenant slug for tenant-scoped writes and branding.                                                                                                                       |
| `KYBERION_TENANT_ID`                          | string                             | no       | Legacy tenant identifier fallback used by peer and surface entrypoints; prefer the canonical KYBERION_TENANT slug and server-resolved viewer scope where available.             |
| `KYBERION_TENANT_SCOPE_REQUIRED`              | string                             | no       | Tenant binding policy for confidential work; strict (or the boolean value 1/true where supported) rejects an unbound tenant, while warn records the migration warning.          |
| `KYBERION_TERMINAL_RESTORE_RUNTIME`           | string                             | no       | Optional runtime setting for terminal restore runtime; leave unset to use the built-in configuration.                                                                           |
| `KYBERION_TIER`                               | string                             | no       | Optional runtime setting for tier; leave unset to use the built-in configuration.                                                                                               |
| `KYBERION_TIMEZONE`                           | string                             | no       | Optional runtime setting for timezone; leave unset to use the built-in configuration.                                                                                           |
| `KYBERION_TTS_ARGS`                           | string                             | no       | Optional comma-separated arguments appended to the shell streaming TTS command; values are trimmed and empty items are ignored.                                                 |
| `KYBERION_TUI_DISABLE_AUDIT`                  | string                             | no       | Optional runtime setting for tui disable audit; leave unset to use the built-in configuration.                                                                                  |
| `KYBERION_UI_LOCALE`                          | string                             | no       | Optional runtime setting for ui locale; leave unset to use the built-in configuration.                                                                                          |
| `KYBERION_VAD`                                | string                             | no       | VAD backend id used by the voice loop; `energy` is built in and unavailable or unknown neural backends degrade to energy with a surfaced reason.                                |
| `KYBERION_VIDEO_RENDER_RUN_MODE`              | string                             | no       | Optional runtime setting for video render run mode; leave unset to use the built-in configuration.                                                                              |
| `KYBERION_VIEWER_PRINCIPAL`                   | string                             | no       | Optional runtime setting for viewer principal; leave unset to use the built-in configuration.                                                                                   |
| `KYBERION_VIEWER_SCOPE`                       | string                             | no       | Chronos viewer tenant audit mode: off, warn, or enforce. Unregistered tenant expansion is denied in every mode; warn records the attempt first. Defaults to warn.               |
| `KYBERION_VOICE_LANGUAGE`                     | string                             | no       | Language hint passed to the Python voice bridge; when unset, the unified locale resolver supplies the language.                                                                 |
| `KYBERION_VOICE_PLAY_BLACKHOLE`               | boolean                            | no       | Set to 1 to send Python voice output through the configured BlackHole playback bridge; it has no effect without KYBERION_BLACKHOLE_SCRIPT.                                      |
| `KYBERION_VOICE_PROFILE_ID`                   | string                             | no       | Voice profile identifier forwarded to the Python/TTS bridge; the Python bridge is not installed unless this value is present.                                                   |
| `KYBERION_VOICE_UPGRADE_ALIAS`                | enum: cloud \| local               | no       | Compatibility alias for the voice upgrade target; cloud selects tier 1 and local selects tier 2 when no explicit CLI target is supplied.                                        |
| `KYBERION_WINDOWS_AI`                         | string                             | no       | Optional runtime setting for windows ai; leave unset to use the built-in configuration.                                                                                         |
| `KYBERION_WINDOWS_AUMID`                      | string                             | no       | Optional runtime setting for windows aumid; leave unset to use the built-in configuration.                                                                                      |
| `KYBERION_WINDOWS_IMAGE_GENERATOR`            | string                             | no       | Optional runtime setting for windows image generator; leave unset to use the built-in configuration.                                                                            |
| `KYBERION_WISDOM_LLM_PROFILE`                 | string                             | no       | Optional runtime setting for wisdom llm profile; leave unset to use the built-in configuration.                                                                                 |
| `KYBERION_WORK_COORDINATION_NAMESPACE`        | string                             | no       | Optional runtime setting for work coordination namespace; leave unset to use the built-in configuration.                                                                        |
