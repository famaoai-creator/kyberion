# 実装台帳 — improvement-plans-2026-07 の実装記録

> **開始**: 2026-07-03 / 実装者: Fable 5
> **配信方式**: 社用端末制約により PR は作らず、変更は作業ツリーに反映し `docs/patches/*.patch` として受け渡す。confidential 漏洩スキャンを各パッチ生成前に実施。
> **方針**: 依存順(Wave 1 の P0 基盤から)。1 計画ごとに 実装 → 検証(build/lint/test)→ パッチ化。判断が必要で止める箇所は、そこまでを反映したパッチを残し、本台帳に「未了・理由・方針」を記録する。

## 進捗

| 計画                                                        | 状態           | 検証                                                                                                                   | パッチ                                                                                                                           | 備考                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ----------------------------------------------------------- | -------------- | ---------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| IP-01 ESLint ガバナンス実効化                               | 実装済(検証中) | eslint 単体で enforcement 確認・pnpm lint 実行中                                                                       | (生成予定)                                                                                                                       | ban は fs/node:fs に限定(child_process は core に30+の正当 spawn があるため別作業)。9件の既知 fs 違反は IP-02 参照の抑制コメントで green 維持                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| メディア出力: HTML→PPTX op ＋ native-pptx-engine 値バグ修正 | 実装済・検証済 | core engine test 51/51・media `deck_from_html` 2/2 pass、生成PPTXをLibreOfficeで実描画確認（PowerPoint破損の主因除去） | 上流取込済 (origin/main 732a17f3)。個別パッチは削除し、未取込分は `branch-delta_vs_origin-main-101b9ad9_2026-08-11.patch` に集約 | ①`media:deck_from_html`(transform op)新設＝レポートHTMLの規約語彙を編集可能ネイティブスライドへ（`html-deck-helpers.ts`／manifest／CAPABILITIES／scripts/html-to-pptx CLI）。②`builders.ts`の2値バグ修正: roundRect `cornerRadius` 非整数→PowerPoint破損 を整数ガイド値に堅牢化、`lineSpacing` の spcPct 100倍小(1300→130000)を倍率/％両対応に正規化。③`convert.ts` の `node:child_process` 直呼びを `safeExec` へ置換（AGENTS.md §1 不変条件）。出力は捕捉されるため中継し、失敗時は stdout/stderr を出して exit 1。デッキ描画は safeExec 既定30秒では足りないため 10分に設定。HTML→PPTX 実機疎通で有効な2スライドPPTX生成を確認。④type-ratchet 対応: テストの `any` をベースラインを上げずに削減。`pptx-engine.test.ts` の `style ... as any` 4件は `PptxStyle` が既に `cornerRadius`/`lineSpacing` を宣言済みで**そもそも不要**だったため削除。`index.test.ts` は `PptxDesignProtocol`/`PptxElement` で走査を型付けし、`handleAction(... as any)` 3件を `as MediaAction` に置換（`as any` 追加=0）。基点更新: origin/main(61b7bf3c) へ `git apply --check` クリーン適用確認（旧 f136caa1 版は破棄）。confidential 漏洩スキャン=0 |

| EV-01〜10 イベント処理統一 | 実装済・検証済 | `pnpm build` 成功／`tsc --noEmit` クリーン／新規・改修テスト **123件**(3回連続安定)／`libs/core` 4873 passed・`scripts` 608 passed／`pnpm check:event-wiring` 違反 **9→0**／`pnpm check:catalogs` OK | 上流取込済 (origin/main 732a17f3)。個別パッチは削除し、未取込分は `branch-delta_vs_origin-main-101b9ad9_2026-08-11.patch` に集約 | ミッション `MSN-EVENT-UNIFY-20260810`。計画は [EVENT_HANDLING_UNIFICATION_PLAN_2026-08-10.ja.md](../developer/improvement-plans-2026-08/EVENT_HANDLING_UNIFICATION_PLAN_2026-08-10.ja.md)。**主題**は「QM-02 の関門に発火源を配線し切る」— 関門(TriggerRunner)は完成済みだが本番でそこを通っていたのは cron 1系統だけだった。EV-10 checker を先に赤で立て(9件)、EV-04/06 で止血、EV-01/05 で失われていた発火を回復、EV-02/03 でデッド宣言を解消、EV-07/09 で観測品質、の順。**計画時の誤りを訂正**: 「reflex は dispatcher 未 bind で発火しない」は調査時の grep 出力切れによる誤認で、実際は `nexus-daemon`(enabled surface)が bind 済み＝**稼働中**。よって EV-03 は撤去ではなく安全化(方針A)に変更し、`{{payload}}` の JSON テキスト文字列置換→**構造的置換**(未信頼 payload が params 構造を変えられない)、actuator 許可リスト、冪等化、監査を追加。**実装中に露出した別欠陥も修正**: `acquireLock` のリトライが `while (elapsed < timeoutMs)` で、`withTriggerLeaderLease` の 1ms 予算を前処理で使い切ると**ロックに一度も触れず false**を返し、呼び出し側が「他リーダー保持中」と誤読してスケジューラ tick を落としていた(最低1回試行を保証)。**設計上の判明事項**: `armWatch` は同一プロセス内 managed child を要求するため detached daemon をハートビート観測する watchdog からは使えず、watch は `acp-mediator` の provider stall(in-process・piped・長寿命で quiet 検知が無かった)へ、wake は watchdog の復旧要求へ配線。`daemon_restart` は score 7 → **`approve`**(sandbox 外の service-manager コマンド／発火中のスケジューラ再起動は重複・欠落を生む)なので無人再起動の executor は意図的に未実装。新規 authority role 3件を登録儀式で追加(`generation_scheduler`／`daemon_watchdog`／`nexus_daemon`。`chronos_gateway` は media-generation への write scope が無く流用不可、`nexus_daemon` は security-policy にはあったが canonical 未登録で反射が本番拒否される状態だった)。`knowledge/_manifest.json` は `pnpm generate:knowledge-index` の結果。**併走セッションの変更は除外済み**(`libs/core/index.ts`・`package.json` の他者 hunk、`authority-role-index.json` の `tools/meet-copilot-extension` 行)。基点 5ab75a1d へクリーン適用確認・confidential 漏洩スキャン=0。**残**: 実運用の初回 janitor 実行で消費済み orchestration queue 456件(2026-06〜07・正本 journal は残存)が削除されるため観測付きで実施すること |

## 判断記録(スコープの明確化)

- **パッチ受け渡しの現行地点 (2026-08-11)**: `origin/main` は 101b9ad9。EV-01〜10 / media HTML→PPTX / tenant-data-activation / meet-copilot 基盤は上流 732a17f3 に取込済みのため、対応する個別パッチは削除した(削除前に全追加行を origin/main と HEAD の双方に突合し、いずれかに存在することを確認)。未取込分は `docs/patches/branch-delta_vs_origin-main-101b9ad9_2026-08-11.patch` に集約 — 51 ファイル / +3381・-109、内包コミット 23 件、基点 101b9ad9 へクリーン適用と型チェック確認済み、confidential 漏洩スキャン clean。**この branch-delta パッチは追跡しない** (`.git/info/exclude`): ブランチの派生物なので commit すると次回の delta が自身を内包して肥大する。同じ理由で併走セッションが生成した `agent-mo-11-alignment-gate_vs_origin-main-101b9ad9_2026-08-11.patch` も追跡から外した(ディスク上には残置)。

- **IP-01 の child_process 除外**: AGENTS.md §1 の不変条件は「ファイル I/O は secure-io、node:fs を直接呼ばない」。libs/core には PTY・ストリーミング音声・CLI バックエンド等で長寿命/ストリーミング `spawn` を使う正当なモジュールが 30 以上あり、safeExec(一発実行)では代替不可。よって IP-01 の ban は **fs/node:fs に限定**し、child_process→safeExec 統制は別途の大規模計画に切り出す(本台帳に別項目として追記予定)。
- **IP-01 と IP-02 の分担**: IP-01 は「enforcement を効かせ、新規の fs 直 import を防ぐ」まで。既存の 9 件の fs 直 import(native engine ×5 + mlx-embedding-backend + scripts 3件)は IP-02(secure-io 実移行、文書生成への影響検証が必要)で修正する。それまでは IP-02 参照の `eslint-disable-next-line` で抑制。
- **tests/** の据え置き**: tests は setup で fs を多用(約40ファイル)。un-ignore すると大量の抑制が必要になり CI ゲートを乱すため、tests は ignore 継続(fs 不変条件の enforcement 対象外)。tools/** も拡張・静的アセットのため据え置き。
