{
  "service_id": "confluence",
  "name": "Confluence Cloud REST API v2 Presets",
  "base_url": "https://{{domain}}.atlassian.net/wiki/api/v2",
  "auth_strategy": "basic",
  "recovery_policy": {
    "fallback_strategy": "sequential_alternatives",
    "retry": {
      "maxRetries": 3,
      "initialDelayMs": 500,
      "maxDelayMs": 10000,
      "factor": 2,
      "jitter": true
    },
    "retryable_categories": ["network", "rate_limit", "timeout", "resource_unavailable"]
  },
  "operations": {
    "get_pages": {
      "path": "pages",
      "method": "GET",
      "description": "Get all pages in the instance.",
      "parameters": {
        "limit": { "type": "number", "default": 25 },
        "cursor": { "type": "string" }
      }
    },
    "get_page": {
      "path": "pages/{{page_id}}",
      "method": "GET",
      "description": "Get a specific page by ID.",
      "parameters": {
        "body_format": { "type": "string", "default": "storage", "description": "Format of the body: storage, atlas_doc_format, view" }
      }
    },
    "create_page": {
      "path": "pages",
      "method": "POST",
      "description": "Create a new page.",
      "parameters": {
        "space_id": { "required": true, "type": "string" },
        "status": { "type": "string", "default": "current" },
        "title": { "required": true, "type": "string" },
        "parentId": { "type": "string" },
        "body": { "required": true, "type": "string" }
      },
      "payload_template": {
        "spaceId": "{{space_id}}",
        "status": "{{status}}",
        "title": "{{title}}",
        "parentId": "{{parentId}}",
        "body": {
          "representation": "storage",
          "value": "{{body}}"
        }
      }
    },
    "update_page": {
      "path": "pages/{{page_id}}",
      "method": "PUT",
      "description": "Update an existing page.",
      "parameters": {
        "title": { "type": "string" },
        "body": { "type": "string" },
        "version": { "required": true, "type": "number", "description": "The new version number (current + 1)" }
      },
      "payload_template": {
        "id": "{{page_id}}",
        "status": "current",
        "title": "{{title}}",
        "body": {
          "representation": "storage",
          "value": "{{body}}"
        },
        "version": { "number": "{{version}}" }
      }
    },
    "get_space_pages": {
      "path": "spaces/{{space_id}}/pages",
      "method": "GET",
      "description": "Get all pages in a specific space."
    },
    "get_spaces": {
      "path": "spaces",
      "method": "GET",
      "description": "Get all spaces."
    },
    "get_space": {
      "path": "spaces/{{space_id}}",
      "method": "GET",
      "description": "Get space details."
    },
    "search_content": {
      "base_url": "https://{{domain}}.atlassian.net/wiki/rest/api",
      "path": "search",
      "method": "GET",
      "description": "Search content using CQL (Confluence Query Language).",
      "parameters": {
        "cql": { "required": true, "type": "string", "description": "CQL query string" },
        "limit": { "type": "number", "default": 25 },
        "start": { "type": "number", "default": 0 }
      }
    },
    "get_content_full": {
      "base_url": "https://{{domain}}.atlassian.net/wiki/rest/api",
      "path": "content/{{page_id}}",
      "method": "GET",
      "description": "Get full page content including body using v1 API.",
      "parameters": {
        "page_id": { "required": true, "type": "string" },
        "expand": { "type": "string", "default": "body.view,children.page,ancestors" }
      }
    },
    "get_page_with_body": {
      "path": "pages/{{page_id}}",
      "method": "GET",
      "description": "Get a specific page with body content.",
      "parameters": {
        "body_format": { "type": "string", "default": "storage" }
      }
    },
    "get_page_children": {
      "path": "pages/{{page_id}}/children",
      "method": "GET",
      "description": "Get child pages of a specific page.",
      "parameters": {
        "limit": { "type": "number", "default": 50 }
      }
    }
  }
}
