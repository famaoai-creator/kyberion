{
  "version": "1.0.0",
  "templates": {
    "default": {
      "required_roles": ["owner", "planner", "implementer", "reviewer"],
      "optional_roles": ["tester"]
    },
    "development": {
      "required_roles": ["owner", "planner", "implementer", "reviewer", "tester"],
      "optional_roles": ["operator", "surface_liaison"]
    },
    "product_development": {
      "required_roles": ["owner", "product_strategist", "planner", "implementer", "reviewer", "tester"],
      "optional_roles": ["experience_designer", "operator", "surface_liaison"]
    },
    "operations": {
      "required_roles": ["owner", "planner", "operator", "reviewer"],
      "optional_roles": ["tester", "surface_liaison"]
    },
    "system_query": {
      "required_roles": ["owner", "planner", "operator"],
      "optional_roles": ["reviewer", "surface_liaison"]
    },
    "incident": {
      "required_roles": ["owner", "operator", "reviewer", "surface_liaison"],
      "optional_roles": ["planner", "tester"]
    }
  }
}
