# test-runners

Placeholder namespace for reusable test-runner knowledge artifacts.

Prefer keeping executable test logic in `scripts/` or `libs/`; use this directory for committed guidance or snapshots only when they are runtime-referenced.
