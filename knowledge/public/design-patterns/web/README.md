# Web Design Patterns

`web-theme-pack` stores the imported visual heritage for a website.

`web-design-system-pack` stores the structural rules that turn that theme into a repeatable web surface.

Use them together when a site should preserve both look and structure instead of re-inventing layout on each build.
