# browser-scenarios

Placeholder namespace for browser scenario assets and curated examples.

Keep this directory only if a runtime reference or catalog entry needs it. Otherwise prefer a documented scenario under `docs/SCENARIO_CATALOG.md` and the root `docs/USE_CASES.md`.
