{
  "$id": "https://kyberion.internal/schemas/governance-catalog.schema.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Governance catalog envelope",
  "description": "Governance envelope. Domain-specific required top-level keys are enforced by governance-catalog-contracts.json until each catalog receives a dedicated JSON Schema.",
  "type": "object",
  "properties": {
    "$schema": { "type": "string" },
    "documentation_only": { "type": "boolean" },
    "version": { "type": ["string", "number"] }
  },
  "required": ["$schema"],
  "additionalProperties": true
}
