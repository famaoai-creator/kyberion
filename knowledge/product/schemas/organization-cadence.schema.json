{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://kyberion.local/schemas/organization-cadence.schema.json",
  "title": "Organization Governance Cadence",
  "type": "object",
  "required": [
    "version",
    "cadence_id",
    "organization_id",
    "name",
    "cadence_type",
    "schedule",
    "owner_role",
    "decision_ids",
    "tier",
    "status",
    "updated_at"
  ],
  "properties": {
    "version": { "type": "string", "minLength": 1 },
    "cadence_id": { "type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$" },
    "organization_id": { "type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$" },
    "name": { "type": "string", "minLength": 1 },
    "cadence_type": {
      "type": "string",
      "enum": ["daily", "weekly", "monthly", "quarterly", "ad_hoc"]
    },
    "schedule": { "type": "string", "minLength": 1 },
    "owner_role": { "type": "string", "minLength": 1 },
    "decision_ids": { "type": "array", "items": { "type": "string" } },
    "tier": { "type": "string", "enum": ["personal", "confidential", "public"] },
    "tenant_slug": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]{1,30}$",
      "not": { "enum": ["public", "confidential", "personal", "shared"] }
    },
    "status": { "type": "string", "enum": ["draft", "active", "paused", "retired"] },
    "updated_at": { "type": "string", "format": "date-time" },
    "metadata": { "type": "object", "additionalProperties": true }
  },
  "additionalProperties": false,
  "allOf": [
    {
      "if": { "required": ["tier"], "properties": { "tier": { "const": "confidential" } } },
      "then": { "required": ["tenant_slug"] }
    }
  ]
}
