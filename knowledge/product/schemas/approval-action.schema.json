{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Approval Actuator Request",
  "type": "object",
  "required": ["action", "params"],
  "properties": {
    "action": { "enum": ["create", "load", "decide", "list_pending", "pipeline"] },
    "params": { "type": "object" }
  }
}
