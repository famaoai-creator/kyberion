{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Knowledge index cache usage map",
  "description": "Last-use timestamps for knowledge index cache scopes.",
  "type": "object",
  "additionalProperties": {
    "type": "string",
    "minLength": 1,
    "format": "date-time"
  }
}
