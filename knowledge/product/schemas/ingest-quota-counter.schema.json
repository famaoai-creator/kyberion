{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://kyberion.local/schemas/ingest-quota-counter.schema.json",
  "title": "Ingest Quota Counter",
  "description": "Tenant and UTC-day bound persisted ingest quota usage.",
  "type": "object",
  "additionalProperties": false,
  "required": ["tenant_slug", "date", "files", "bytes", "updated_at"],
  "properties": {
    "tenant_slug": { "type": "string", "minLength": 1 },
    "date": { "type": "string", "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$" },
    "files": { "type": "integer", "minimum": 0 },
    "bytes": { "type": "integer", "minimum": 0 },
    "updated_at": { "type": "string", "format": "date-time" }
  }
}
