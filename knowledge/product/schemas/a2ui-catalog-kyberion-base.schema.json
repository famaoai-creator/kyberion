{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://kyberion.ai/schemas/a2ui-catalog-kyberion-base.schema.json",
  "title": "A2UI catalog kyberion-base",
  "description": "Props contract for the shared kyberion-base A2UI component catalog (UI-01). Validates one component {id?, type, props, children?}; $defs['props:<type>'] is the props schema for each ui:* type. Legacy aliases (text/button/card/container) and display:*/kb-*/presence.* types are outside this catalog.",
  "type": "object",
  "properties": {
    "id": {
      "$ref": "#/$defs/id"
    },
    "type": {
      "enum": [
        "ui:app-shell",
        "ui:page-header",
        "ui:nav-rail",
        "ui:tabs",
        "ui:stack",
        "ui:grid",
        "ui:section",
        "ui:next-action",
        "ui:metric",
        "ui:kv",
        "ui:table",
        "ui:list",
        "ui:text",
        "ui:code",
        "ui:status-pill",
        "ui:badge",
        "ui:callout",
        "ui:empty-state",
        "ui:skeleton",
        "ui:button",
        "ui:disclosure",
        "ui:display-controls",
        "ui:settings-group",
        "ui:setting-row",
        "ui:switch",
        "ui:checkbox",
        "ui:select",
        "ui:radio-group",
        "ui:segmented",
        "ui:text-field",
        "ui:textarea",
        "ui:slider",
        "ui:integration-item",
        "ui:save-bar",
        "ui:file-drop",
        "ui:camera-capture",
        "ui:avatar-picker",
        "ui:secret-field",
        "ui:bar-chart",
        "ui:line-chart",
        "ui:donut",
        "ui:sparkline",
        "ui:heatmap",
        "ui:meter",
        "ui:sequence",
        "ui:flow",
        "ui:stat-list",
        "ui:voice-input",
        "ui:voice-state",
        "ui:toolbar",
        "ui:dialog",
        "ui:drawing-palette",
        "ui:sketch-board",
        "ui:talking-avatar"
      ]
    },
    "props": {
      "type": "object"
    },
    "children": {
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  },
  "required": ["type", "props"],
  "additionalProperties": false,
  "allOf": [
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:app-shell"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:app-shell"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:page-header"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:page-header"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:nav-rail"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:nav-rail"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:tabs"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:tabs"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:stack"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:stack"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:grid"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:grid"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:section"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:section"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:next-action"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:next-action"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:metric"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:metric"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:kv"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:kv"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:table"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:table"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:list"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:list"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:text"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:text"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:code"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:code"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:status-pill"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:status-pill"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:badge"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:badge"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:callout"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:callout"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:empty-state"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:empty-state"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:skeleton"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:skeleton"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:button"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:button"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:disclosure"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:disclosure"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:display-controls"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:display-controls"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:settings-group"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:settings-group"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:setting-row"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:setting-row"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:switch"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:switch"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:checkbox"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:checkbox"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:select"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:select"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:radio-group"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:radio-group"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:segmented"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:segmented"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:text-field"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:text-field"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:textarea"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:textarea"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:slider"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:slider"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:integration-item"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:integration-item"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:save-bar"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:save-bar"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:file-drop"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:file-drop"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:camera-capture"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:camera-capture"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:avatar-picker"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:avatar-picker"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:secret-field"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:secret-field"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:bar-chart"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:bar-chart"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:line-chart"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:line-chart"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:donut"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:donut"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:sparkline"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:sparkline"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:heatmap"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:heatmap"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:meter"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:meter"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:sequence"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:sequence"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:flow"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:flow"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:stat-list"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:stat-list"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:voice-input"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:voice-input"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:voice-state"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:voice-state"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:toolbar"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:toolbar"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:dialog"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:dialog"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:drawing-palette"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:drawing-palette"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:sketch-board"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:sketch-board"
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "type": {
            "const": "ui:talking-avatar"
          }
        },
        "required": ["type"]
      },
      "then": {
        "properties": {
          "props": {
            "$ref": "#/$defs/props:ui:talking-avatar"
          }
        }
      }
    }
  ],
  "$defs": {
    "text": {
      "type": "string",
      "minLength": 1,
      "maxLength": 200
    },
    "long-text": {
      "type": "string",
      "maxLength": 4000
    },
    "id": {
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9:._-]{0,80}$"
    },
    "href": {
      "type": "string",
      "minLength": 1,
      "maxLength": 2048,
      "pattern": "^(?![\\s]*[jJ][aA][vV][aA][sS][cC][rR][iI][pP][tT]:)(?![\\s]*[dD][aA][tT][aA]:)",
      "description": "Navigation target. javascript: and data: URLs are rejected."
    },
    "action": {
      "type": "object",
      "description": "Surface-dispatched action (the renderer routes it; never evaluated as code).",
      "properties": {
        "id": {
          "$ref": "#/$defs/id"
        },
        "payload": {
          "type": "object"
        }
      },
      "required": ["id"],
      "additionalProperties": false
    },
    "tone": {
      "enum": ["neutral", "accent", "info", "success", "warning", "danger"]
    },
    "callout-tone": {
      "enum": ["info", "success", "warning", "danger"]
    },
    "surface-role": {
      "enum": [
        "concierge",
        "presence-studio",
        "chronos-mirror-v2",
        "operator-surface",
        "computer-surface"
      ]
    },
    "density": {
      "enum": ["comfortable", "compact"]
    },
    "gap": {
      "enum": ["none", "xs", "sm", "md", "lg", "xl"]
    },
    "status": {
      "enum": [
        "active",
        "archived",
        "available",
        "blocked",
        "busy",
        "completed",
        "connected",
        "connecting",
        "degraded",
        "disconnected",
        "distilling",
        "done",
        "error",
        "failed",
        "fallback",
        "fully_automatable",
        "missing",
        "missing_runtime_prerequisites",
        "n/a",
        "needs_assets",
        "needs_clarification",
        "needs_external_assets",
        "needs_runtime_prerequisites",
        "needs_setup",
        "offline",
        "paused",
        "pending",
        "planned",
        "ready",
        "recovered",
        "review",
        "running",
        "stale",
        "stopped",
        "unavailable",
        "working"
      ],
      "description": "Canonical dashboard status vocabulary (libs/core/ux-vocabulary.ts renderStatus domains)."
    },
    "status-domain": {
      "enum": ["readiness", "connection", "provider", "mission", "progress", "runtime"]
    },
    "button-variant": {
      "enum": ["primary", "secondary", "danger", "ghost"]
    },
    "action-ref": {
      "type": "object",
      "description": "A labelled link or action. Exactly one of href / action.",
      "properties": {
        "label": {
          "$ref": "#/$defs/text"
        },
        "href": {
          "$ref": "#/$defs/href"
        },
        "action": {
          "anyOf": [
            {
              "$ref": "#/$defs/action"
            },
            {
              "$ref": "#/$defs/id"
            }
          ],
          "description": "Surface-dispatched action, or just its id (no payload)."
        },
        "variant": {
          "$ref": "#/$defs/button-variant"
        },
        "disabled": {
          "type": "boolean"
        }
      },
      "required": ["label"],
      "additionalProperties": false,
      "oneOf": [
        {
          "required": ["href"]
        },
        {
          "required": ["action"]
        }
      ]
    },
    "scalar": {
      "type": ["string", "number", "boolean", "null"]
    },
    "table-cell": {
      "description": "ui:table cell: a scalar, or a rich cell — {title, id?, href?} (title with a mono id line), {status, label?, domain?} (status pill) or {badge, tone?} (badge).",
      "anyOf": [
        {
          "$ref": "#/$defs/scalar"
        },
        {
          "type": "object",
          "properties": {
            "title": {
              "$ref": "#/$defs/text"
            },
            "id": {
              "type": "string",
              "maxLength": 200
            },
            "href": {
              "$ref": "#/$defs/href"
            }
          },
          "required": ["title"],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "status": {
              "$ref": "#/$defs/status"
            },
            "label": {
              "type": "string",
              "maxLength": 80
            },
            "domain": {
              "$ref": "#/$defs/status-domain"
            }
          },
          "required": ["status"],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "badge": {
              "type": "string",
              "minLength": 1,
              "maxLength": 80
            },
            "tone": {
              "$ref": "#/$defs/tone"
            }
          },
          "required": ["badge"],
          "additionalProperties": false
        }
      ]
    },
    "nav-item": {
      "type": "object",
      "properties": {
        "id": {
          "$ref": "#/$defs/id"
        },
        "label": {
          "$ref": "#/$defs/text"
        },
        "hint": {
          "type": "string",
          "maxLength": 200
        },
        "href": {
          "$ref": "#/$defs/href"
        },
        "action": {
          "anyOf": [
            {
              "$ref": "#/$defs/action"
            },
            {
              "$ref": "#/$defs/id"
            }
          ],
          "description": "Dispatched on activation. Without href the item is a button; with href a plain click dispatches instead of navigating."
        },
        "icon": {
          "type": "string",
          "pattern": "^[a-z0-9-]{1,40}$"
        },
        "active": {
          "type": "boolean"
        }
      },
      "required": ["id", "label"],
      "anyOf": [
        {
          "required": ["href"]
        },
        {
          "required": ["action"]
        }
      ],
      "additionalProperties": false
    },
    "props:ui:app-shell": {
      "type": "object",
      "description": "Page skeleton. Children: nav-rail, page-header, main content.",
      "properties": {
        "density": {
          "$ref": "#/$defs/density"
        },
        "theme": {
          "enum": ["system", "light", "dark"]
        },
        "role": {
          "$ref": "#/$defs/surface-role"
        }
      },
      "additionalProperties": false
    },
    "props:ui:page-header": {
      "type": "object",
      "properties": {
        "title": {
          "$ref": "#/$defs/text"
        },
        "subtitle": {
          "type": "string",
          "maxLength": 400
        },
        "role_badge": {
          "type": "object",
          "properties": {
            "label": {
              "$ref": "#/$defs/text"
            },
            "tone": {
              "$ref": "#/$defs/tone"
            },
            "role": {
              "$ref": "#/$defs/surface-role"
            }
          },
          "required": ["label"],
          "additionalProperties": false
        },
        "actions": {
          "type": "array",
          "maxItems": 4,
          "items": {
            "$ref": "#/$defs/action-ref"
          }
        }
      },
      "required": ["title"],
      "additionalProperties": false
    },
    "props:ui:nav-rail": {
      "type": "object",
      "properties": {
        "label": {
          "type": "string",
          "maxLength": 80
        },
        "brand": {
          "type": "object",
          "description": "Brand slot at the top of the rail: product / surface name, optional subtitle and logo image (a token-colored mark when no logo).",
          "properties": {
            "name": {
              "$ref": "#/$defs/text"
            },
            "subtitle": {
              "type": "string",
              "maxLength": 200
            },
            "logo_url": {
              "$ref": "#/$defs/href"
            }
          },
          "required": ["name"],
          "additionalProperties": false
        },
        "context": {
          "type": "object",
          "description": "Context block (tenant / workspace being viewed). options + action = switcher (action payload gets { value }); action alone = button; href alone = link.",
          "properties": {
            "label": {
              "$ref": "#/$defs/text"
            },
            "detail": {
              "type": "string",
              "maxLength": 200
            },
            "switch_label": {
              "type": "string",
              "maxLength": 120
            },
            "href": {
              "$ref": "#/$defs/href"
            },
            "action": {
              "$ref": "#/$defs/action"
            },
            "options": {
              "type": "array",
              "maxItems": 50,
              "items": {
                "type": "object",
                "properties": {
                  "value": {
                    "type": "string",
                    "minLength": 1,
                    "maxLength": 200
                  },
                  "label": {
                    "$ref": "#/$defs/text"
                  },
                  "selected": {
                    "type": "boolean"
                  }
                },
                "required": ["value", "label"],
                "additionalProperties": false
              }
            }
          },
          "required": ["label"],
          "additionalProperties": false
        },
        "items": {
          "type": "array",
          "maxItems": 24,
          "items": {
            "$ref": "#/$defs/nav-item"
          }
        },
        "footer_items": {
          "type": "array",
          "maxItems": 8,
          "items": {
            "$ref": "#/$defs/nav-item"
          }
        }
      },
      "required": ["items"],
      "additionalProperties": false
    },
    "props:ui:tabs": {
      "type": "object",
      "properties": {
        "items": {
          "type": "array",
          "minItems": 1,
          "maxItems": 24,
          "items": {
            "type": "object",
            "properties": {
              "id": {
                "$ref": "#/$defs/id"
              },
              "label": {
                "$ref": "#/$defs/text"
              },
              "count": {
                "type": "integer",
                "minimum": 0
              },
              "href": {
                "$ref": "#/$defs/href"
              }
            },
            "required": ["id", "label"],
            "additionalProperties": false
          }
        },
        "active": {
          "$ref": "#/$defs/id"
        },
        "overflow": {
          "enum": ["wrap", "menu"]
        },
        "variant": {
          "enum": ["primary", "secondary"],
          "description": "primary (default) = underlined page tabs; secondary = pill-style sub-navigation."
        }
      },
      "required": ["items"],
      "additionalProperties": false
    },
    "props:ui:stack": {
      "type": "object",
      "properties": {
        "gap": {
          "$ref": "#/$defs/gap"
        },
        "direction": {
          "enum": ["vertical", "horizontal"]
        },
        "align": {
          "enum": ["start", "center", "end", "stretch"]
        },
        "wrap": {
          "type": "boolean"
        }
      },
      "additionalProperties": false
    },
    "props:ui:grid": {
      "type": "object",
      "properties": {
        "gap": {
          "$ref": "#/$defs/gap"
        },
        "columns": {
          "type": "integer",
          "minimum": 1,
          "maximum": 6,
          "description": "Fixed column count; omit for auto-fit."
        },
        "min_column_width": {
          "enum": ["xs", "sm", "md", "lg"]
        }
      },
      "additionalProperties": false
    },
    "props:ui:section": {
      "type": "object",
      "properties": {
        "title": {
          "type": "string",
          "maxLength": 200
        },
        "description": {
          "type": "string",
          "maxLength": 1000
        },
        "tone": {
          "$ref": "#/$defs/tone"
        },
        "actions": {
          "type": "array",
          "maxItems": 4,
          "items": {
            "$ref": "#/$defs/action-ref"
          }
        }
      },
      "additionalProperties": false
    },
    "props:ui:next-action": {
      "type": "object",
      "description": "The single most important next step. primary is required unless state is loading or empty.",
      "properties": {
        "eyebrow": {
          "type": "string",
          "maxLength": 80
        },
        "title": {
          "$ref": "#/$defs/text"
        },
        "reason": {
          "type": "string",
          "maxLength": 1000
        },
        "primary": {
          "$ref": "#/$defs/action-ref"
        },
        "secondary": {
          "$ref": "#/$defs/action-ref"
        },
        "state": {
          "enum": ["ready", "loading", "empty"]
        }
      },
      "required": ["title"],
      "additionalProperties": false,
      "if": {
        "properties": {
          "state": {
            "enum": ["loading", "empty"]
          }
        },
        "required": ["state"]
      },
      "else": {
        "required": ["primary"]
      }
    },
    "props:ui:metric": {
      "type": "object",
      "properties": {
        "label": {
          "$ref": "#/$defs/text"
        },
        "value": {
          "type": ["string", "number"]
        },
        "unit": {
          "type": "string",
          "maxLength": 20
        },
        "delta": {
          "type": "string",
          "maxLength": 40
        },
        "trend": {
          "enum": ["up", "down", "flat"]
        },
        "tone": {
          "$ref": "#/$defs/tone"
        },
        "description": {
          "type": "string",
          "maxLength": 400
        }
      },
      "required": ["label", "value"],
      "additionalProperties": false
    },
    "props:ui:kv": {
      "type": "object",
      "properties": {
        "items": {
          "type": "array",
          "maxItems": 64,
          "items": {
            "type": "object",
            "properties": {
              "label": {
                "$ref": "#/$defs/text"
              },
              "value": {
                "type": ["string", "number", "boolean"]
              },
              "mono": {
                "type": "boolean"
              }
            },
            "required": ["label", "value"],
            "additionalProperties": false
          }
        }
      },
      "required": ["items"],
      "additionalProperties": false
    },
    "props:ui:table": {
      "type": "object",
      "properties": {
        "caption": {
          "type": "string",
          "maxLength": 200
        },
        "columns": {
          "type": "array",
          "minItems": 1,
          "maxItems": 24,
          "items": {
            "type": "object",
            "properties": {
              "key": {
                "type": "string",
                "pattern": "^[A-Za-z0-9_.-]{1,64}$"
              },
              "label": {
                "type": "string",
                "maxLength": 120
              },
              "align": {
                "enum": ["start", "center", "end"]
              },
              "mono": {
                "type": "boolean"
              },
              "width": {
                "type": "string",
                "pattern": "^(auto|[0-9]{1,4}(px|rem|ch|%))$"
              }
            },
            "required": ["key", "label"],
            "additionalProperties": false
          }
        },
        "rows": {
          "type": "array",
          "maxItems": 1000,
          "items": {
            "type": "object",
            "additionalProperties": {
              "$ref": "#/$defs/table-cell"
            }
          }
        },
        "row_href_key": {
          "type": "string",
          "pattern": "^[A-Za-z0-9_.-]{1,64}$"
        },
        "empty": {
          "type": "string",
          "maxLength": 400
        }
      },
      "required": ["columns", "rows"],
      "additionalProperties": false
    },
    "props:ui:list": {
      "type": "object",
      "properties": {
        "items": {
          "type": "array",
          "maxItems": 200,
          "items": {
            "type": "object",
            "properties": {
              "title": {
                "$ref": "#/$defs/text"
              },
              "meta": {
                "type": "string",
                "maxLength": 400
              },
              "status": {
                "$ref": "#/$defs/status"
              },
              "status_label": {
                "type": "string",
                "minLength": 1,
                "maxLength": 80,
                "description": "Visible status pill text (default: the localized status label)."
              },
              "progress": {
                "type": "number",
                "minimum": 0,
                "maximum": 100,
                "description": "Percent complete, drawn as a small meter with accessible text."
              },
              "href": {
                "$ref": "#/$defs/href"
              },
              "action": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/action"
                  },
                  {
                    "$ref": "#/$defs/id"
                  }
                ],
                "description": "Dispatched when the title is activated (the title becomes a button, or a link whose plain click dispatches)."
              },
              "actions": {
                "type": "array",
                "maxItems": 4,
                "items": {
                  "$ref": "#/$defs/action-ref"
                },
                "description": "Trailing row actions (e.g. Delete), described by the row title."
              }
            },
            "required": ["title"],
            "additionalProperties": false
          }
        },
        "variant": {
          "enum": ["plain", "timeline"]
        }
      },
      "required": ["items"],
      "additionalProperties": false
    },
    "props:ui:text": {
      "type": "object",
      "properties": {
        "text": {
          "type": "string",
          "maxLength": 8000
        },
        "variant": {
          "enum": ["body", "muted", "caption", "mono", "title"]
        }
      },
      "required": ["text"],
      "additionalProperties": false
    },
    "props:ui:code": {
      "type": "object",
      "description": "Monospace code / log block (white-space: pre-wrap). No syntax highlighting; the text is never interpreted.",
      "properties": {
        "code": {
          "type": "string",
          "maxLength": 20000
        },
        "language": {
          "type": "string",
          "pattern": "^[A-Za-z0-9+#._-]{1,40}$"
        },
        "title": {
          "$ref": "#/$defs/text"
        }
      },
      "required": ["code"],
      "additionalProperties": false
    },
    "props:ui:status-pill": {
      "type": "object",
      "description": "Icon + text status indicator; never color-only.",
      "properties": {
        "status": {
          "$ref": "#/$defs/status"
        },
        "domain": {
          "$ref": "#/$defs/status-domain"
        },
        "label": {
          "type": "string",
          "maxLength": 80
        }
      },
      "required": ["status"],
      "additionalProperties": false
    },
    "props:ui:badge": {
      "type": "object",
      "properties": {
        "label": {
          "$ref": "#/$defs/text"
        },
        "tone": {
          "$ref": "#/$defs/tone"
        },
        "role": {
          "$ref": "#/$defs/surface-role"
        }
      },
      "required": ["label"],
      "additionalProperties": false
    },
    "props:ui:callout": {
      "type": "object",
      "properties": {
        "tone": {
          "$ref": "#/$defs/callout-tone"
        },
        "title": {
          "$ref": "#/$defs/text"
        },
        "body": {
          "$ref": "#/$defs/long-text"
        },
        "action": {
          "$ref": "#/$defs/action-ref"
        }
      },
      "required": ["tone", "title"],
      "additionalProperties": false
    },
    "props:ui:empty-state": {
      "type": "object",
      "properties": {
        "title": {
          "$ref": "#/$defs/text"
        },
        "body": {
          "$ref": "#/$defs/long-text"
        },
        "action": {
          "$ref": "#/$defs/action-ref"
        }
      },
      "required": ["title"],
      "additionalProperties": false
    },
    "props:ui:skeleton": {
      "type": "object",
      "properties": {
        "lines": {
          "type": "integer",
          "minimum": 1,
          "maximum": 12
        },
        "shape": {
          "enum": ["text", "card", "table"]
        }
      },
      "additionalProperties": false
    },
    "props:ui:button": {
      "type": "object",
      "properties": {
        "label": {
          "$ref": "#/$defs/text"
        },
        "variant": {
          "$ref": "#/$defs/button-variant"
        },
        "href": {
          "$ref": "#/$defs/href"
        },
        "action": {
          "$ref": "#/$defs/action"
        },
        "disabled": {
          "type": "boolean"
        },
        "title": {
          "$ref": "#/$defs/text"
        },
        "target": {
          "type": "string",
          "enum": ["_blank", "_self"],
          "description": "Link target, only meaningful with href. _blank always gets rel=\"noopener\"."
        }
      },
      "required": ["label"],
      "additionalProperties": false,
      "oneOf": [
        {
          "required": ["href"]
        },
        {
          "required": ["action"]
        }
      ]
    },
    "props:ui:disclosure": {
      "type": "object",
      "description": "Collapsed-by-default region (developer-only detail, advanced settings).",
      "properties": {
        "summary": {
          "$ref": "#/$defs/text"
        },
        "open": {
          "type": "boolean"
        }
      },
      "required": ["summary"],
      "additionalProperties": false
    },
    "props:ui:display-controls": {
      "type": "object",
      "description": "Viewer display preferences: theme (system / light / dark) segmented control + language select. Dispatches display.theme / display.locale with { value }; the host persists.",
      "properties": {
        "theme": {
          "enum": ["system", "light", "dark"]
        },
        "locale": {
          "type": "string",
          "pattern": "^[a-z]{2,3}(-[A-Za-z0-9]{2,8})*$"
        },
        "locales": {
          "type": "array",
          "minItems": 1,
          "maxItems": 8,
          "items": {
            "type": "object",
            "properties": {
              "value": {
                "type": "string",
                "pattern": "^[a-z]{2,3}(-[A-Za-z0-9]{2,8})*$"
              },
              "label": {
                "$ref": "#/$defs/text"
              }
            },
            "required": ["value", "label"],
            "additionalProperties": false
          }
        }
      },
      "additionalProperties": false
    },
    "field-name": {
      "type": "string",
      "pattern": "^[A-Za-z0-9_][A-Za-z0-9_.:-]{0,80}$",
      "description": "Field key reported in field.change / action payloads."
    },
    "field-help": {
      "type": "string",
      "minLength": 1,
      "maxLength": 400
    },
    "field-option": {
      "type": "object",
      "properties": {
        "value": {
          "type": "string",
          "maxLength": 200
        },
        "label": {
          "$ref": "#/$defs/text"
        },
        "description": {
          "type": "string",
          "maxLength": 400
        },
        "disabled": {
          "type": "boolean"
        }
      },
      "required": ["value", "label"],
      "additionalProperties": false
    },
    "field-options": {
      "type": "array",
      "minItems": 1,
      "maxItems": 100,
      "items": {
        "$ref": "#/$defs/field-option"
      }
    },
    "file-entry": {
      "type": "object",
      "description": "Metadata of one selected file. File contents are never props.",
      "properties": {
        "id": {
          "$ref": "#/$defs/id"
        },
        "name": {
          "type": "string",
          "minLength": 1,
          "maxLength": 255
        },
        "size": {
          "type": "integer",
          "minimum": 0
        },
        "status": {
          "enum": ["queued", "ready", "uploading", "done", "error"]
        },
        "progress": {
          "type": "number",
          "minimum": 0,
          "maximum": 100
        },
        "error": {
          "$ref": "#/$defs/field-help"
        }
      },
      "required": ["id", "name", "status"],
      "additionalProperties": false
    },
    "props:ui:settings-group": {
      "type": "object",
      "description": "Titled group of settings. Children: ui:setting-row (or any component).",
      "properties": {
        "title": {
          "$ref": "#/$defs/text"
        },
        "description": {
          "$ref": "#/$defs/long-text"
        }
      },
      "required": ["title"],
      "additionalProperties": false
    },
    "props:ui:setting-row": {
      "type": "object",
      "description": "One setting: label + description on the left, one control (child) on the right.",
      "properties": {
        "label": {
          "$ref": "#/$defs/text"
        },
        "description": {
          "$ref": "#/$defs/long-text"
        },
        "tone": {
          "enum": ["neutral", "danger"]
        }
      },
      "required": ["label"],
      "additionalProperties": false
    },
    "props:ui:switch": {
      "type": "object",
      "description": "On/off toggle (role=switch). Edits dispatch field.change {name, value}.",
      "properties": {
        "name": {
          "$ref": "#/$defs/field-name"
        },
        "label": {
          "$ref": "#/$defs/text"
        },
        "hide_label": {
          "type": "boolean"
        },
        "help": {
          "$ref": "#/$defs/field-help"
        },
        "error": {
          "$ref": "#/$defs/field-help"
        },
        "required": {
          "type": "boolean"
        },
        "disabled": {
          "type": "boolean"
        },
        "value": {
          "type": "boolean"
        }
      },
      "required": ["name", "label"],
      "additionalProperties": false
    },
    "props:ui:checkbox": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/field-name"
        },
        "label": {
          "$ref": "#/$defs/text"
        },
        "hide_label": {
          "type": "boolean"
        },
        "help": {
          "$ref": "#/$defs/field-help"
        },
        "error": {
          "$ref": "#/$defs/field-help"
        },
        "required": {
          "type": "boolean"
        },
        "disabled": {
          "type": "boolean"
        },
        "value": {
          "type": "boolean"
        }
      },
      "required": ["name", "label"],
      "additionalProperties": false
    },
    "props:ui:select": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/field-name"
        },
        "label": {
          "$ref": "#/$defs/text"
        },
        "hide_label": {
          "type": "boolean"
        },
        "help": {
          "$ref": "#/$defs/field-help"
        },
        "error": {
          "$ref": "#/$defs/field-help"
        },
        "required": {
          "type": "boolean"
        },
        "disabled": {
          "type": "boolean"
        },
        "value": {
          "type": "string",
          "maxLength": 200
        },
        "options": {
          "$ref": "#/$defs/field-options"
        },
        "placeholder": {
          "$ref": "#/$defs/text"
        }
      },
      "required": ["name", "label", "options"],
      "additionalProperties": false
    },
    "props:ui:radio-group": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/field-name"
        },
        "label": {
          "$ref": "#/$defs/text"
        },
        "hide_label": {
          "type": "boolean"
        },
        "help": {
          "$ref": "#/$defs/field-help"
        },
        "error": {
          "$ref": "#/$defs/field-help"
        },
        "required": {
          "type": "boolean"
        },
        "disabled": {
          "type": "boolean"
        },
        "value": {
          "type": "string",
          "maxLength": 200
        },
        "options": {
          "$ref": "#/$defs/field-options"
        },
        "direction": {
          "enum": ["vertical", "horizontal"]
        }
      },
      "required": ["name", "label", "options"],
      "additionalProperties": false
    },
    "props:ui:segmented": {
      "type": "object",
      "description": "Small exclusive choice rendered as a segmented radio group.",
      "properties": {
        "name": {
          "$ref": "#/$defs/field-name"
        },
        "label": {
          "$ref": "#/$defs/text"
        },
        "hide_label": {
          "type": "boolean"
        },
        "help": {
          "$ref": "#/$defs/field-help"
        },
        "error": {
          "$ref": "#/$defs/field-help"
        },
        "required": {
          "type": "boolean"
        },
        "disabled": {
          "type": "boolean"
        },
        "value": {
          "type": "string",
          "maxLength": 200
        },
        "options": {
          "type": "array",
          "minItems": 2,
          "maxItems": 6,
          "items": {
            "$ref": "#/$defs/field-option"
          }
        }
      },
      "required": ["name", "label", "options"],
      "additionalProperties": false
    },
    "props:ui:text-field": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/field-name"
        },
        "label": {
          "$ref": "#/$defs/text"
        },
        "hide_label": {
          "type": "boolean"
        },
        "help": {
          "$ref": "#/$defs/field-help"
        },
        "error": {
          "$ref": "#/$defs/field-help"
        },
        "required": {
          "type": "boolean"
        },
        "disabled": {
          "type": "boolean"
        },
        "value": {
          "type": ["string", "number"],
          "maxLength": 4000
        },
        "type": {
          "enum": ["text", "email", "url", "number", "search"]
        },
        "placeholder": {
          "$ref": "#/$defs/text"
        },
        "maxlength": {
          "type": "integer",
          "minimum": 1,
          "maximum": 4000
        },
        "min": {
          "type": "number"
        },
        "max": {
          "type": "number"
        },
        "step": {
          "type": "number",
          "exclusiveMinimum": 0
        },
        "readonly": {
          "type": "boolean"
        }
      },
      "required": ["name", "label"],
      "additionalProperties": false
    },
    "props:ui:textarea": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/field-name"
        },
        "label": {
          "$ref": "#/$defs/text"
        },
        "hide_label": {
          "type": "boolean"
        },
        "help": {
          "$ref": "#/$defs/field-help"
        },
        "error": {
          "$ref": "#/$defs/field-help"
        },
        "required": {
          "type": "boolean"
        },
        "disabled": {
          "type": "boolean"
        },
        "value": {
          "type": "string",
          "maxLength": 20000
        },
        "placeholder": {
          "$ref": "#/$defs/text"
        },
        "maxlength": {
          "type": "integer",
          "minimum": 1,
          "maximum": 20000
        },
        "rows": {
          "type": "integer",
          "minimum": 2,
          "maximum": 30
        },
        "readonly": {
          "type": "boolean"
        }
      },
      "required": ["name", "label"],
      "additionalProperties": false
    },
    "props:ui:slider": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/field-name"
        },
        "label": {
          "$ref": "#/$defs/text"
        },
        "hide_label": {
          "type": "boolean"
        },
        "help": {
          "$ref": "#/$defs/field-help"
        },
        "error": {
          "$ref": "#/$defs/field-help"
        },
        "required": {
          "type": "boolean"
        },
        "disabled": {
          "type": "boolean"
        },
        "value": {
          "type": "number"
        },
        "min": {
          "type": "number"
        },
        "max": {
          "type": "number"
        },
        "step": {
          "type": "number",
          "exclusiveMinimum": 0
        },
        "unit": {
          "type": "string",
          "maxLength": 20
        }
      },
      "required": ["name", "label"],
      "additionalProperties": false
    },
    "props:ui:integration-item": {
      "type": "object",
      "description": "External connection status (connected / needs re-auth / disconnected / error) with actions.",
      "properties": {
        "title": {
          "$ref": "#/$defs/text"
        },
        "state": {
          "enum": ["connected", "needs_reauth", "disconnected", "error"]
        },
        "description": {
          "$ref": "#/$defs/long-text"
        },
        "detail": {
          "type": "string",
          "maxLength": 200
        },
        "icon": {
          "type": "string",
          "pattern": "^[a-z0-9-]{1,40}$"
        },
        "actions": {
          "type": "array",
          "maxItems": 3,
          "items": {
            "$ref": "#/$defs/action-ref"
          }
        }
      },
      "required": ["title", "state"],
      "additionalProperties": false
    },
    "props:ui:save-bar": {
      "type": "object",
      "description": "Sticky unsaved-changes bar (save / discard).",
      "properties": {
        "state": {
          "enum": ["clean", "dirty", "saving", "saved", "error"]
        },
        "message": {
          "type": "string",
          "maxLength": 400
        },
        "save_action": {
          "$ref": "#/$defs/action"
        },
        "discard_action": {
          "$ref": "#/$defs/action"
        },
        "save_label": {
          "$ref": "#/$defs/text"
        },
        "discard_label": {
          "$ref": "#/$defs/text"
        }
      },
      "required": ["state", "save_action"],
      "additionalProperties": false
    },
    "props:ui:file-drop": {
      "type": "object",
      "description": "Drag & drop / click / paste file picker. Selected files reach the host only as the onAction payload; `files` lists metadata the host tracks (progress, errors).",
      "properties": {
        "name": {
          "$ref": "#/$defs/field-name"
        },
        "label": {
          "$ref": "#/$defs/text"
        },
        "hide_label": {
          "type": "boolean"
        },
        "help": {
          "$ref": "#/$defs/field-help"
        },
        "error": {
          "$ref": "#/$defs/field-help"
        },
        "required": {
          "type": "boolean"
        },
        "disabled": {
          "type": "boolean"
        },
        "description": {
          "$ref": "#/$defs/long-text"
        },
        "accept": {
          "type": "string",
          "maxLength": 400,
          "pattern": "^[A-Za-z0-9.+*/_ ,-]*$"
        },
        "multiple": {
          "type": "boolean"
        },
        "max_bytes": {
          "type": "integer",
          "minimum": 1
        },
        "max_files": {
          "type": "integer",
          "minimum": 1,
          "maximum": 100
        },
        "files": {
          "type": "array",
          "maxItems": 100,
          "items": {
            "$ref": "#/$defs/file-entry"
          }
        },
        "action": {
          "$ref": "#/$defs/action"
        },
        "remove_action": {
          "$ref": "#/$defs/action"
        }
      },
      "required": ["name", "label"],
      "additionalProperties": false
    },
    "props:ui:camera-capture": {
      "type": "object",
      "description": "Camera photo capture started by an explicit user action; falls back to a file input. The photo reaches the host only as the onAction payload.",
      "properties": {
        "name": {
          "$ref": "#/$defs/field-name"
        },
        "label": {
          "$ref": "#/$defs/text"
        },
        "hide_label": {
          "type": "boolean"
        },
        "help": {
          "$ref": "#/$defs/field-help"
        },
        "error": {
          "$ref": "#/$defs/field-help"
        },
        "required": {
          "type": "boolean"
        },
        "disabled": {
          "type": "boolean"
        },
        "description": {
          "$ref": "#/$defs/long-text"
        },
        "facing": {
          "enum": ["user", "environment"]
        },
        "aspect": {
          "enum": ["square", "landscape"]
        },
        "action": {
          "$ref": "#/$defs/action"
        },
        "cancel_action": {
          "$ref": "#/$defs/action"
        }
      },
      "required": ["name", "label"],
      "additionalProperties": false
    },
    "props:ui:avatar-picker": {
      "type": "object",
      "description": "Current avatar + upload / take photo / remove with a square crop preview. Images reach the host only as the onAction payload.",
      "properties": {
        "name": {
          "$ref": "#/$defs/field-name"
        },
        "label": {
          "$ref": "#/$defs/text"
        },
        "hide_label": {
          "type": "boolean"
        },
        "help": {
          "$ref": "#/$defs/field-help"
        },
        "error": {
          "$ref": "#/$defs/field-help"
        },
        "required": {
          "type": "boolean"
        },
        "disabled": {
          "type": "boolean"
        },
        "image_url": {
          "$ref": "#/$defs/href"
        },
        "initials": {
          "type": "string",
          "maxLength": 3
        },
        "shape": {
          "enum": ["circle", "square"]
        },
        "allow_camera": {
          "type": "boolean"
        },
        "removable": {
          "type": "boolean"
        },
        "action": {
          "$ref": "#/$defs/action"
        },
        "remove_action": {
          "$ref": "#/$defs/action"
        }
      },
      "required": ["name", "label"],
      "additionalProperties": false
    },
    "props:ui:secret-field": {
      "type": "object",
      "description": "Secret (API token) input. There is deliberately no `value` prop: a stored secret is never sent to the page, only `configured` + `last4`. The typed value reaches the host only in the submit action payload.",
      "properties": {
        "name": {
          "$ref": "#/$defs/field-name"
        },
        "label": {
          "$ref": "#/$defs/text"
        },
        "hide_label": {
          "type": "boolean"
        },
        "help": {
          "$ref": "#/$defs/field-help"
        },
        "error": {
          "$ref": "#/$defs/field-help"
        },
        "required": {
          "type": "boolean"
        },
        "disabled": {
          "type": "boolean"
        },
        "action": {
          "$ref": "#/$defs/action"
        },
        "remove_action": {
          "$ref": "#/$defs/action"
        },
        "configured": {
          "type": "boolean"
        },
        "last4": {
          "type": "string",
          "pattern": "^[A-Za-z0-9]{1,4}$"
        },
        "placeholder": {
          "$ref": "#/$defs/text"
        },
        "service_id": {
          "$ref": "#/$defs/id"
        },
        "secret_key": {
          "type": "string",
          "pattern": "^[A-Za-z0-9_.-]{1,80}$"
        },
        "status": {
          "description": "Host-reported outcome of the last submit. The field reads 'sending' after a submit until the host sets saved or error; it never reports success on its own. error reopens the input.",
          "enum": ["idle", "pending", "error", "saved"]
        },
        "status_error": {
          "$ref": "#/$defs/field-help",
          "description": "Host-localized reason shown instead of the generic error text when status is error."
        }
      },
      "required": ["name", "label", "action"],
      "additionalProperties": false
    },
    "chart-datum": {
      "type": "object",
      "properties": {
        "label": {
          "type": "string",
          "maxLength": 200
        },
        "value": {
          "type": ["number", "null"]
        }
      },
      "required": ["label", "value"],
      "additionalProperties": false
    },
    "props:ui:bar-chart": {
      "type": "object",
      "description": "UI-01b: grouped / stacked bars, vertical or horizontal. `series` + `categories`, or the single-series `data` shorthand (chronos display:bar-chart compatible).",
      "properties": {
        "title": {
          "$ref": "#/$defs/text"
        },
        "description": {
          "type": "string",
          "maxLength": 400
        },
        "density": {
          "$ref": "#/$defs/density"
        },
        "empty": {
          "type": "string",
          "maxLength": 200
        },
        "categories": {
          "type": "array",
          "maxItems": 200,
          "items": {
            "type": "string",
            "maxLength": 200
          }
        },
        "series": {
          "type": "array",
          "maxItems": 24,
          "items": {
            "type": "object",
            "properties": {
              "name": {
                "$ref": "#/$defs/text"
              },
              "values": {
                "type": "array",
                "maxItems": 200,
                "items": {
                  "type": ["number", "null"]
                }
              }
            },
            "required": ["name", "values"],
            "additionalProperties": false
          }
        },
        "data": {
          "type": "array",
          "maxItems": 200,
          "items": {
            "$ref": "#/$defs/chart-datum"
          }
        },
        "orientation": {
          "enum": ["vertical", "horizontal"]
        },
        "stacked": {
          "type": "boolean"
        },
        "unit": {
          "type": "string",
          "maxLength": 20
        },
        "show_values": {
          "type": "boolean"
        }
      },
      "anyOf": [
        {
          "required": ["series"]
        },
        {
          "required": ["data"]
        }
      ],
      "additionalProperties": false
    },
    "props:ui:line-chart": {
      "type": "object",
      "description": "UI-01b: time series as lines (or areas). `x` is a label (point scale) or a number (linear scale).",
      "properties": {
        "title": {
          "$ref": "#/$defs/text"
        },
        "description": {
          "type": "string",
          "maxLength": 400
        },
        "density": {
          "$ref": "#/$defs/density"
        },
        "empty": {
          "type": "string",
          "maxLength": 200
        },
        "series": {
          "type": "array",
          "maxItems": 24,
          "items": {
            "type": "object",
            "properties": {
              "name": {
                "$ref": "#/$defs/text"
              },
              "points": {
                "type": "array",
                "maxItems": 500,
                "items": {
                  "type": "object",
                  "properties": {
                    "x": {
                      "type": ["string", "number"]
                    },
                    "y": {
                      "type": ["number", "null"]
                    }
                  },
                  "required": ["x", "y"],
                  "additionalProperties": false
                }
              }
            },
            "required": ["name", "points"],
            "additionalProperties": false
          }
        },
        "area": {
          "type": "boolean"
        },
        "y_unit": {
          "type": "string",
          "maxLength": 20
        }
      },
      "required": ["series"],
      "additionalProperties": false
    },
    "props:ui:donut": {
      "type": "object",
      "description": "UI-01b: part-to-whole ring; more than 8 segments fold into Other. `data` is the chronos display:donut shorthand.",
      "properties": {
        "title": {
          "$ref": "#/$defs/text"
        },
        "description": {
          "type": "string",
          "maxLength": 400
        },
        "density": {
          "$ref": "#/$defs/density"
        },
        "empty": {
          "type": "string",
          "maxLength": 200
        },
        "segments": {
          "type": "array",
          "maxItems": 50,
          "items": {
            "$ref": "#/$defs/chart-datum"
          }
        },
        "data": {
          "type": "array",
          "maxItems": 50,
          "items": {
            "$ref": "#/$defs/chart-datum"
          }
        },
        "center_label": {
          "type": "string",
          "maxLength": 40
        },
        "center_value": {
          "type": ["string", "number"]
        },
        "unit": {
          "type": "string",
          "maxLength": 20
        }
      },
      "anyOf": [
        {
          "required": ["segments"]
        },
        {
          "required": ["data"]
        }
      ],
      "additionalProperties": false
    },
    "props:ui:sparkline": {
      "type": "object",
      "description": "UI-01b: inline trend (row / metric card).",
      "properties": {
        "points": {
          "type": "array",
          "maxItems": 500,
          "items": {
            "type": ["number", "null"]
          }
        },
        "label": {
          "type": "string",
          "maxLength": 200
        },
        "tone": {
          "$ref": "#/$defs/tone"
        },
        "unit": {
          "type": "string",
          "maxLength": 20
        },
        "show_value": {
          "type": "boolean"
        },
        "empty": {
          "type": "string",
          "maxLength": 200
        }
      },
      "required": ["points"],
      "additionalProperties": false
    },
    "props:ui:heatmap": {
      "type": "object",
      "description": "UI-01b: density grid (e.g. weekday x hour). Sequential scale by default; `diverging` centres on `midpoint` (default 0).",
      "properties": {
        "title": {
          "$ref": "#/$defs/text"
        },
        "description": {
          "type": "string",
          "maxLength": 400
        },
        "density": {
          "$ref": "#/$defs/density"
        },
        "empty": {
          "type": "string",
          "maxLength": 200
        },
        "rows": {
          "type": "array",
          "maxItems": 200,
          "items": {
            "type": "string",
            "maxLength": 200
          }
        },
        "columns": {
          "type": "array",
          "maxItems": 200,
          "items": {
            "type": "string",
            "maxLength": 200
          }
        },
        "values": {
          "type": "array",
          "maxItems": 200,
          "items": {
            "type": "array",
            "maxItems": 200,
            "items": {
              "type": ["number", "null"]
            }
          }
        },
        "unit": {
          "type": "string",
          "maxLength": 20
        },
        "scale": {
          "enum": ["sequential", "diverging"]
        },
        "midpoint": {
          "type": "number"
        },
        "show_values": {
          "type": "boolean"
        }
      },
      "required": ["rows", "columns", "values"],
      "additionalProperties": false
    },
    "props:ui:meter": {
      "type": "object",
      "description": "UI-01b: usage against a limit. The tone is the highest threshold at or below `value`; it is shown with a glyph and a state word, never color alone.",
      "properties": {
        "label": {
          "type": "string",
          "maxLength": 200
        },
        "value": {
          "type": ["number", "null"]
        },
        "max": {
          "type": "number",
          "exclusiveMinimum": 0
        },
        "unit": {
          "type": "string",
          "maxLength": 20
        },
        "thresholds": {
          "type": "array",
          "maxItems": 6,
          "items": {
            "type": "object",
            "properties": {
              "value": {
                "type": "number"
              },
              "tone": {
                "enum": ["success", "warning", "danger"]
              }
            },
            "required": ["value", "tone"],
            "additionalProperties": false
          }
        },
        "direction": {
          "enum": ["higher_is_better", "lower_is_better"],
          "description": "lower_is_better (default; usage against a limit) or higher_is_better (rates such as completion). Only the state words change; the tone is always the highest threshold at or below value."
        },
        "description": {
          "type": "string",
          "maxLength": 400
        },
        "empty": {
          "type": "string",
          "maxLength": 200
        }
      },
      "required": ["value"],
      "additionalProperties": false
    },
    "props:ui:sequence": {
      "type": "object",
      "description": "UI-01b: message flow between participant lanes over time. `kind: reply` draws a dashed arrow; from === to is a self-message; `status` adds a status glyph + label.",
      "properties": {
        "title": {
          "$ref": "#/$defs/text"
        },
        "description": {
          "type": "string",
          "maxLength": 400
        },
        "density": {
          "$ref": "#/$defs/density"
        },
        "empty": {
          "type": "string",
          "maxLength": 200
        },
        "participants": {
          "type": "array",
          "maxItems": 12,
          "items": {
            "anyOf": [
              {
                "type": "string",
                "minLength": 1,
                "maxLength": 120
              },
              {
                "type": "object",
                "properties": {
                  "id": {
                    "type": "string",
                    "minLength": 1,
                    "maxLength": 120
                  },
                  "label": {
                    "$ref": "#/$defs/text"
                  }
                },
                "required": ["id"],
                "additionalProperties": false
              }
            ]
          }
        },
        "messages": {
          "type": "array",
          "maxItems": 200,
          "items": {
            "type": "object",
            "properties": {
              "from": {
                "type": "string",
                "minLength": 1,
                "maxLength": 120
              },
              "to": {
                "type": "string",
                "minLength": 1,
                "maxLength": 120
              },
              "label": {
                "type": "string",
                "maxLength": 200
              },
              "at": {
                "type": ["string", "number"]
              },
              "status": {
                "$ref": "#/$defs/status"
              },
              "kind": {
                "enum": ["call", "reply", "note"]
              }
            },
            "required": ["from", "to", "label"],
            "additionalProperties": false
          }
        }
      },
      "required": ["participants", "messages"],
      "additionalProperties": false
    },
    "props:ui:flow": {
      "type": "object",
      "description": "UI-01b: staged DAG (mission -> tasks -> agents). Layers are `stage`s when every node has one, else longest-path layers; ordering is deterministic.",
      "properties": {
        "title": {
          "$ref": "#/$defs/text"
        },
        "description": {
          "type": "string",
          "maxLength": 400
        },
        "density": {
          "$ref": "#/$defs/density"
        },
        "empty": {
          "type": "string",
          "maxLength": 200
        },
        "nodes": {
          "type": "array",
          "maxItems": 200,
          "items": {
            "type": "object",
            "properties": {
              "id": {
                "type": "string",
                "minLength": 1,
                "maxLength": 120
              },
              "label": {
                "$ref": "#/$defs/text"
              },
              "stage": {
                "type": "string",
                "minLength": 1,
                "maxLength": 120
              },
              "status": {
                "$ref": "#/$defs/status"
              },
              "meta": {
                "type": "string",
                "maxLength": 120
              }
            },
            "required": ["id", "label"],
            "additionalProperties": false
          }
        },
        "edges": {
          "type": "array",
          "maxItems": 400,
          "items": {
            "type": "object",
            "properties": {
              "from": {
                "type": "string",
                "minLength": 1,
                "maxLength": 120
              },
              "to": {
                "type": "string",
                "minLength": 1,
                "maxLength": 120
              },
              "label": {
                "type": "string",
                "maxLength": 80
              }
            },
            "required": ["from", "to"],
            "additionalProperties": false
          }
        },
        "stages": {
          "type": "array",
          "maxItems": 20,
          "items": {
            "anyOf": [
              {
                "type": "string",
                "minLength": 1,
                "maxLength": 120
              },
              {
                "type": "object",
                "properties": {
                  "id": {
                    "type": "string",
                    "minLength": 1,
                    "maxLength": 120
                  },
                  "label": {
                    "$ref": "#/$defs/text"
                  }
                },
                "required": ["id"],
                "additionalProperties": false
              }
            ]
          }
        }
      },
      "required": ["nodes"],
      "additionalProperties": false
    },
    "props:ui:stat-list": {
      "type": "object",
      "description": "UI-01b: summary statistics (mean, median, p95, ...).",
      "properties": {
        "title": {
          "$ref": "#/$defs/text"
        },
        "density": {
          "$ref": "#/$defs/density"
        },
        "empty": {
          "type": "string",
          "maxLength": 200
        },
        "items": {
          "type": "array",
          "maxItems": 50,
          "items": {
            "type": "object",
            "properties": {
              "label": {
                "$ref": "#/$defs/text"
              },
              "value": {
                "type": ["string", "number"]
              },
              "unit": {
                "type": "string",
                "maxLength": 20
              },
              "hint": {
                "type": "string",
                "maxLength": 200
              }
            },
            "required": ["label", "value"],
            "additionalProperties": false
          }
        }
      },
      "required": ["items"],
      "additionalProperties": false
    },
    "props:ui:voice-input": {
      "type": "object",
      "description": "PA-02 microphone input with a live level meter, elapsed time and (dictation) interim transcript. The mic opens only from a user action; audio files and transcripts reach the host only as onAction payloads (voice.state / voice.transcript / voice.recording / voice.error).",
      "properties": {
        "name": {
          "$ref": "#/$defs/field-name"
        },
        "label": {
          "$ref": "#/$defs/text"
        },
        "hide_label": {
          "type": "boolean"
        },
        "help": {
          "$ref": "#/$defs/field-help"
        },
        "disabled": {
          "type": "boolean"
        },
        "mode": {
          "enum": ["dictation", "record"]
        },
        "lang": {
          "type": "string",
          "pattern": "^[A-Za-z]{2,3}(-[A-Za-z0-9]{2,8})*$",
          "maxLength": 35
        },
        "continuous": {
          "type": "boolean"
        },
        "push_to_talk": {
          "type": "boolean"
        },
        "show_level": {
          "type": "boolean"
        },
        "show_transcript": {
          "type": "boolean"
        },
        "chunk_ms": {
          "type": "integer",
          "minimum": 100,
          "maximum": 600000
        },
        "max_seconds": {
          "type": "number",
          "exclusiveMinimum": 0,
          "maximum": 3600
        },
        "status": {
          "enum": ["idle", "transcribing", "error"]
        },
        "status_error": {
          "$ref": "#/$defs/field-help"
        },
        "actions": {
          "type": "object",
          "properties": {
            "state": {
              "$ref": "#/$defs/action"
            },
            "transcript": {
              "$ref": "#/$defs/action"
            },
            "recording": {
              "$ref": "#/$defs/action"
            },
            "error": {
              "$ref": "#/$defs/action"
            }
          },
          "additionalProperties": false
        }
      },
      "required": ["name", "label"],
      "additionalProperties": false
    },
    "props:ui:voice-state": {
      "type": "object",
      "description": "PA-02 display-only conversation state (idle / listening / thinking / speaking / muted / error) with an optional host-driven level 0..1. Reused by the talking avatar.",
      "properties": {
        "state": {
          "enum": ["idle", "listening", "thinking", "speaking", "muted", "error"]
        },
        "label": {
          "$ref": "#/$defs/text"
        },
        "level": {
          "type": "number",
          "minimum": 0,
          "maximum": 1
        },
        "variant": {
          "enum": ["bars", "orb", "dot"]
        },
        "size": {
          "enum": ["sm", "md", "lg"]
        },
        "show_label": {
          "type": "boolean"
        }
      },
      "required": ["state"],
      "additionalProperties": false
    },
    "drawing-tool": {
      "enum": ["pen", "highlighter", "rect", "ellipse", "line", "arrow", "text", "eraser"]
    },
    "drawing-tools": {
      "type": "array",
      "minItems": 1,
      "maxItems": 8,
      "uniqueItems": true,
      "items": {
        "$ref": "#/$defs/drawing-tool"
      }
    },
    "hex-color": {
      "type": "string",
      "pattern": "^#(?:[0-9A-Fa-f]{3}|[0-9A-Fa-f]{6})$"
    },
    "drawing-colors": {
      "type": "array",
      "minItems": 1,
      "maxItems": 24,
      "items": {
        "$ref": "#/$defs/hex-color"
      }
    },
    "drawing-width": {
      "type": "integer",
      "minimum": 1,
      "maximum": 48
    },
    "props:ui:toolbar": {
      "type": "object",
      "description": "PA-01 WAI-ARIA toolbar (roving tabindex, arrow keys) of buttons / toggles / file pickers / separators / spacers / status text. Dispatches toolbar.click {id}, toolbar.toggle {id, pressed}, toolbar.files {id, files} (File objects only in the payload) unless an item declares its own action.",
      "properties": {
        "label": {
          "$ref": "#/$defs/text"
        },
        "items": {
          "type": "array",
          "maxItems": 40,
          "items": {
            "type": "object",
            "properties": {
              "type": {
                "enum": ["button", "toggle", "file", "separator", "spacer", "status"]
              },
              "id": {
                "$ref": "#/$defs/id"
              },
              "label": {
                "$ref": "#/$defs/text"
              },
              "icon": {
                "type": "string",
                "minLength": 1,
                "maxLength": 8
              },
              "hide_label": {
                "type": "boolean"
              },
              "description": {
                "$ref": "#/$defs/field-help"
              },
              "variant": {
                "$ref": "#/$defs/button-variant"
              },
              "pressed": {
                "type": "boolean"
              },
              "disabled": {
                "type": "boolean"
              },
              "action": {
                "$ref": "#/$defs/action"
              },
              "accept": {
                "type": "string",
                "maxLength": 400,
                "pattern": "^[A-Za-z0-9.+*/_ ,-]*$"
              },
              "multiple": {
                "type": "boolean"
              },
              "text": {
                "$ref": "#/$defs/text"
              },
              "tone": {
                "$ref": "#/$defs/tone"
              }
            },
            "required": ["type"],
            "additionalProperties": false,
            "allOf": [
              {
                "if": {
                  "properties": {
                    "type": {
                      "enum": ["button", "toggle", "file"]
                    }
                  }
                },
                "then": {
                  "required": ["id", "label"]
                }
              },
              {
                "if": {
                  "properties": {
                    "type": {
                      "const": "status"
                    }
                  }
                },
                "then": {
                  "required": ["text"]
                }
              }
            ]
          }
        },
        "sticky": {
          "type": "boolean"
        },
        "density": {
          "$ref": "#/$defs/density"
        }
      },
      "required": ["label", "items"],
      "additionalProperties": false
    },
    "props:ui:dialog": {
      "type": "object",
      "description": "PA-01 modal dialog replacing window.confirm() / window.prompt(): confirm, single input, or several choices. Controlled by open. Dispatches action (default dialog.confirm) with {choice?, value?} and cancel_action (default dialog.cancel) on Cancel / Escape. The component's children render in the body between the message / input and the buttons (focus trap included).",
      "properties": {
        "open": {
          "type": "boolean"
        },
        "title": {
          "$ref": "#/$defs/text"
        },
        "message": {
          "$ref": "#/$defs/long-text"
        },
        "tone": {
          "enum": ["neutral", "danger"]
        },
        "input": {
          "type": "object",
          "properties": {
            "name": {
              "$ref": "#/$defs/field-name"
            },
            "label": {
              "$ref": "#/$defs/text"
            },
            "placeholder": {
              "type": "string",
              "maxLength": 200
            },
            "value": {
              "type": "string",
              "maxLength": 4000
            },
            "multiline": {
              "type": "boolean"
            }
          },
          "required": ["name", "label"],
          "additionalProperties": false
        },
        "choices": {
          "type": "array",
          "minItems": 1,
          "maxItems": 4,
          "items": {
            "type": "object",
            "properties": {
              "id": {
                "$ref": "#/$defs/id"
              },
              "label": {
                "$ref": "#/$defs/text"
              },
              "variant": {
                "$ref": "#/$defs/button-variant"
              }
            },
            "required": ["id", "label"],
            "additionalProperties": false
          }
        },
        "confirm_label": {
          "$ref": "#/$defs/text"
        },
        "cancel_label": {
          "$ref": "#/$defs/text"
        },
        "action": {
          "$ref": "#/$defs/action"
        },
        "cancel_action": {
          "$ref": "#/$defs/action"
        }
      },
      "required": ["open", "title"],
      "additionalProperties": false
    },
    "props:ui:drawing-palette": {
      "type": "object",
      "description": "PA-01 controlled drawing palette: tools, color swatches (+ optional custom color), stroke width, undo / clear. Dispatches drawing.change {name, tool? | color? | width?}, drawing.undo {name}, drawing.clear {name}.",
      "properties": {
        "name": {
          "$ref": "#/$defs/field-name"
        },
        "label": {
          "$ref": "#/$defs/text"
        },
        "tools": {
          "$ref": "#/$defs/drawing-tools"
        },
        "tool": {
          "$ref": "#/$defs/drawing-tool"
        },
        "colors": {
          "$ref": "#/$defs/drawing-colors"
        },
        "color": {
          "$ref": "#/$defs/hex-color"
        },
        "allow_custom_color": {
          "type": "boolean"
        },
        "width": {
          "$ref": "#/$defs/drawing-width"
        },
        "min_width": {
          "$ref": "#/$defs/drawing-width"
        },
        "max_width": {
          "$ref": "#/$defs/drawing-width"
        },
        "can_undo": {
          "type": "boolean"
        },
        "show_clear": {
          "type": "boolean"
        },
        "orientation": {
          "enum": ["horizontal", "vertical"]
        }
      },
      "required": ["name", "label"],
      "additionalProperties": false
    },
    "props:ui:sketch-board": {
      "type": "object",
      "description": "PA-01 stateful sketch board (palette + canvas): pointer drawing with live shape preview, inline text input, undo, clear confirmation, PNG download. Dispatches drawing.ready {name, controller} on mount, drawing.change {name, dirty, strokes} after each stroke / undo / clear, drawing.background {name, file} for a dropped / pasted image (File only in the payload).",
      "properties": {
        "name": {
          "$ref": "#/$defs/field-name"
        },
        "label": {
          "$ref": "#/$defs/text"
        },
        "tools": {
          "$ref": "#/$defs/drawing-tools"
        },
        "colors": {
          "$ref": "#/$defs/drawing-colors"
        },
        "default_tool": {
          "$ref": "#/$defs/drawing-tool"
        },
        "default_color": {
          "$ref": "#/$defs/hex-color"
        },
        "default_width": {
          "$ref": "#/$defs/drawing-width"
        },
        "min_width": {
          "$ref": "#/$defs/drawing-width"
        },
        "max_width": {
          "$ref": "#/$defs/drawing-width"
        },
        "allow_custom_color": {
          "type": "boolean"
        },
        "canvas_width": {
          "type": "integer",
          "minimum": 64,
          "maximum": 4096
        },
        "canvas_height": {
          "type": "integer",
          "minimum": 64,
          "maximum": 4096
        },
        "background": {
          "enum": ["dark", "light", "transparent"]
        },
        "background_image_url": {
          "$ref": "#/$defs/href"
        },
        "accept_image_drop": {
          "type": "boolean"
        },
        "max_undo": {
          "type": "integer",
          "minimum": 1,
          "maximum": 200
        },
        "show_download": {
          "type": "boolean"
        },
        "download_name": {
          "type": "string",
          "minLength": 1,
          "maxLength": 120,
          "pattern": "^[^\\\\/:*?\"<>|\\u0000-\\u001f]+$",
          "description": "File name of the PNG download (default: from name); .png is ensured."
        },
        "paste_scope": {
          "enum": ["board", "document"],
          "description": "With accept_image_drop: board (default) takes pasted images while the board has focus; document anywhere on the page except editable fields."
        }
      },
      "required": ["name", "label"],
      "additionalProperties": false
    },
    "avatar-image-url": {
      "type": "string",
      "minLength": 1,
      "maxLength": 2048,
      "pattern": "^(?:[hH][tT][tT][pP][sS]?://[^\\s]+|(?![a-zA-Z][a-zA-Z0-9+.-]*:)(?!//)(?!\\\\)[^\\s]+)$",
      "description": "Same-origin path or http(s) URL of an avatar frame. data:, blob: and every other scheme are rejected (images are never inline in props)."
    },
    "avatar-fraction": {
      "type": "number",
      "minimum": 0,
      "maximum": 1
    },
    "props:ui:talking-avatar": {
      "type": "object",
      "description": "PA-09 talking avatar: layered expression images with crossfade, a lip-synced mouth (overlay SVG at the mouth anchor, or an open-mouth frame blended by openness), idle blink / breathing (off under reduced motion) and an optional ui:voice-state indicator. Mouth motion never comes from props: avatar.ready {name, controller} hands the host setLevel / applyCue (AnimationCue shape) / attachAnalyser / startSynthetic / pulse / setExpression / setState / dispose.",
      "properties": {
        "name": {
          "$ref": "#/$defs/field-name"
        },
        "label": {
          "$ref": "#/$defs/text"
        },
        "images": {
          "type": "object",
          "properties": {
            "neutral": {
              "$ref": "#/$defs/avatar-image-url"
            },
            "joy": {
              "$ref": "#/$defs/avatar-image-url"
            },
            "thinking": {
              "$ref": "#/$defs/avatar-image-url"
            },
            "listening": {
              "$ref": "#/$defs/avatar-image-url"
            },
            "speaking": {
              "$ref": "#/$defs/avatar-image-url"
            },
            "mouth_open": {
              "$ref": "#/$defs/avatar-image-url"
            },
            "blink": {
              "$ref": "#/$defs/avatar-image-url"
            }
          },
          "required": ["neutral"],
          "additionalProperties": false
        },
        "expression": {
          "enum": ["neutral", "joy", "thinking", "listening"]
        },
        "state": {
          "enum": ["idle", "listening", "thinking", "speaking", "muted", "error"]
        },
        "show_state": {
          "type": "boolean"
        },
        "mouth": {
          "type": "object",
          "properties": {
            "x": {
              "$ref": "#/$defs/avatar-fraction"
            },
            "y": {
              "$ref": "#/$defs/avatar-fraction"
            },
            "width": {
              "type": "number",
              "minimum": 0.02,
              "maximum": 1
            }
          },
          "additionalProperties": false
        },
        "mouth_mode": {
          "enum": ["auto", "overlay", "frames"]
        },
        "size": {
          "enum": ["sm", "md", "lg", "xl"]
        },
        "shape": {
          "enum": ["circle", "rounded"]
        },
        "fallback_initials": {
          "type": "string",
          "minLength": 1,
          "maxLength": 3
        }
      },
      "required": ["name", "label", "images"],
      "additionalProperties": false
    }
  }
}
