{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Blockchain Actuator Request",
  "type": "object",
  "required": ["action", "params"],
  "properties": {
    "action": { "enum": ["anchor_mission", "anchor_trust", "verify_anchor"] },
    "params": { "type": "object" }
  }
}
