{
  "$schema": "../../schemas/seam-provider-selection-policy.schema.json",
  "version": "1.0.0",
  "seams": [
    {
      "seam_id": "music-generation-provider",
      "default_provider": "stable_audio_3",
      "fallback_purpose": "quality",
      "traits": {
        "quality": "Musical fidelity and prompt adherence of the generated clip.",
        "speed": "Wall-clock from request to a saved clip, including model start-up."
      },
      "providers": {
        "musicgen_mlx": {
          "traits": { "quality": 0.5, "speed": 0.5 },
          "trait_basis": { "quality": "declared", "speed": "declared" },
          "evidence": [
            "libs/core/music-generation-bridge.ts (LocalMusicGenMlxGenerationProvider: local_only, self_hosted, Apple Silicon only, wav, max duration from KYBERION_MUSICGEN_MAX_DURATION_SEC, default 30s)",
            "Neutral declared values: no comparison with stable_audio_3 exists yet. Differentiate with `pnpm kyberion seam select calibrate --seam music-generation-provider` and record the result (rules set / apply-measurements)."
          ]
        },
        "stable_audio_3": {
          "traits": { "quality": 0.5, "speed": 0.5 },
          "trait_basis": { "quality": "declared", "speed": "declared" },
          "evidence": [
            "libs/core/music-generation-bridge.ts (LocalStableAudioGenerationProvider: local_only, self_hosted, any platform, wav, max duration from KYBERION_STABLE_AUDIO_MAX_DURATION_SEC, default 120s)",
            "Neutral declared values: no comparison with musicgen_mlx exists yet. Differentiate with `pnpm kyberion seam select calibrate --seam music-generation-provider` and record the result (rules set / apply-measurements)."
          ]
        }
      },
      "purposes": {
        "quality": {
          "description": "Musical fidelity matters most (final soundtracks, deliverables).",
          "weights": { "quality": 1 }
        },
        "speed": {
          "description": "Drafts and previews where time to a saved clip matters most.",
          "weights": { "speed": 0.8, "quality": 0.2 }
        }
      }
    }
  ]
}
