{
  "$schema": "../../schemas/capability-validation-contract.schema.json",
  "rules": [
    {
      "name": "actuator-metadata-completeness",
      "check": {
        "required_fields": ["name", "description", "status"],
        "valid_statuses": ["implemented", "planned", "conceptual", "unstable"],
        "required_files": ["package.json", "src/index.ts"]
      }
    }
  ]
}
