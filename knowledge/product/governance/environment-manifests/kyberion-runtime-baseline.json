{
  "$schema": "https://kyberion.local/schemas/environment-capability-manifest.schema.json",
  "manifest_id": "kyberion-runtime-baseline",
  "version": "2026-07-05",
  "description": "The minimum host requirements for *anything* in Kyberion to run: Node, pnpm, git, and a built workspace. Every other manifest implicitly depends on this one being satisfied first.",
  "capabilities": [
    {
      "capability_id": "node-runtime",
      "kind": "binary",
      "description": "Node.js v24+ on PATH (the only runtime Kyberion targets; must satisfy package.json engines). Older versions miss native fetch+stream APIs the reasoning backends rely on.",
      "required_for": ["all-of-kyberion"],
      "probe": { "kind": "probe", "probe_id": "node-version.floor" },
      "install": {
        "operator_confirmed": true,
        "instruction": "Install Node.js 24+ via your version manager: `nvm install 24 && nvm use 24` or `mise install node@24` (matches `.nvmrc` and package.json engines). Operator-confirmed because the version manager change can affect other projects.",
        "docs_url": "https://nodejs.org/en/download"
      }
    },
    {
      "capability_id": "pnpm",
      "kind": "binary",
      "description": "pnpm package manager on PATH; `npm` and `yarn` are not supported.",
      "required_for": ["all-of-kyberion"],
      "probe": { "kind": "command", "command": "pnpm", "args": ["--version"] },
      "install": {
        "operator_confirmed": true,
        "command": "corepack",
        "args": ["enable", "pnpm"],
        "instruction": "Use Corepack (`corepack enable pnpm`) or `npm i -g pnpm`. Pinned version: `pnpm@11.13.0` (Node.js 24+ required).",
        "docs_url": "https://pnpm.io/installation",
        "retry_after_install": true
      }
    },
    {
      "capability_id": "git",
      "kind": "binary",
      "description": "git on PATH; KSMC creates a per-mission repository, so without git missions cannot start.",
      "required_for": ["all-of-kyberion", "mission-controller"],
      "probe": { "kind": "command", "command": "git", "args": ["--version"] },
      "install": {
        "operator_confirmed": true,
        "instruction": "macOS: `brew install git`. Debian/Ubuntu: `apt-get install -y git`. Other platforms: see the docs link.",
        "docs_url": "https://git-scm.com/downloads"
      }
    },
    {
      "capability_id": "repo-build",
      "kind": "npm-package",
      "description": "The TypeScript build under `libs/core/dist/` is fresh enough that imports of @agent/core resolve at runtime. Missing dist means anything that requires `@agent/core/<sub>` will fail.",
      "required_for": ["all-of-kyberion"],
      "probe": { "kind": "probe", "probe_id": "repo-build.receipt" },
      "install": {
        "operator_confirmed": false,
        "command": "pnpm",
        "args": ["build"],
        "instruction": "Runs the workspace build (`pnpm build`). Non-operator-confirmed because it is fully reversible and writes only to `dist/`.",
        "retry_after_install": true
      }
    }
  ]
}
