{
  "service_id": "github-mcp",
  "name": "GitHub MCP Server (deprecated example)",
  "deprecated": true,
  "canonical_preset": "github",
  "description": "Deprecated external-MCP example using the retired @modelcontextprotocol/server-github package (3 tools). Canonical GitHub work uses the REST preset knowledge/product/orchestration/service-presets/github.json (service_id=github). Keep this file only as a fixture for service-actuator MCP-mode tests and as a pattern for wiring a third-party MCP server — do not add daily GitHub capture or review writes here.",
  "operations": {
    "search_repositories": {
      "type": "mcp",
      "kind": "capture",
      "risk": "read",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "tool_name": "search_repositories",
      "parameters": {
        "query": { "required": true, "type": "string" }
      }
    },
    "get_file_contents": {
      "type": "mcp",
      "kind": "capture",
      "risk": "read",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "tool_name": "get_file_contents",
      "parameters": {
        "owner": { "required": true, "type": "string" },
        "repo": { "required": true, "type": "string" },
        "path": { "required": true, "type": "string" }
      }
    },
    "create_issue": {
      "type": "mcp",
      "kind": "apply",
      "risk": "write",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "tool_name": "create_issue",
      "parameters": {
        "owner": { "required": true, "type": "string" },
        "repo": { "required": true, "type": "string" },
        "title": { "required": true, "type": "string" },
        "body": { "type": "string" }
      }
    }
  }
}
