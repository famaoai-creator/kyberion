{
  "name": "diagram-renderer",
  "version": "1.0.0",
  "description": "Text to Image diagram renderer",
  "main": "scripts/main.cjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "private": true,
  "author": "Gemini Agent",
  "license": "MIT",
  "devDependencies": {
    "@agent/core": "workspace:*"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
