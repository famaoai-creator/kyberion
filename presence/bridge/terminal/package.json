{
  "name": "@presence/terminal-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Terminal bridge service workspace for Kyberion runtime sessions",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "express": "^5.2.1",
    "node-pty": "^1.1.0",
    "ws": "^8.19.0"
  }
}
