[
  {
    "id": "baseline-check",
    "pipeline": "pipelines/baseline-check.json",
    "note": "Session-start health check. The shape of its output is stable: any change is either intentional or a regression."
  },
  {
    "id": "vital-check",
    "pipeline": "pipelines/vital-check.json",
    "note": "System vitals probe. Verifies that the dependency resolver, doctor script, and layer checks produce a consistent status shape.",
    "ignore_paths": ["context.active_missions", "context.timestamp"]
  }
]
