{
  "action": "pipeline",
  "name": "capability-docs-sync-fragment",
  "description": "Synchronizes the global CAPABILITIES_GUIDE.md with all registered CLI and Gateway capabilities across knowledge tiers.",
  "steps": [
    {
      "id": "sync_docs",
      "type": "apply",
      "op": "system:shell",
      "params": {
        "cmd": "node dist/scripts/docs_sync.js",
        "env": { "KYBERION_PERSONA": "sovereign" }
      }
    },
    {
      "id": "log_sync",
      "type": "apply",
      "op": "system:log",
      "params": {
        "message": "CAPABILITIES_GUIDE.md has been synchronized with the latest registry state."
      }
    }
  ]
}
