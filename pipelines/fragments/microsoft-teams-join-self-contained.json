{
  "name": "microsoft-teams-join-self-contained",
  "version": "2.0.0",
  "description": "Self-contained wrapper that primes a browser session and then drives the Microsoft Teams join page until the join button is pressed. Accepts meeting_url, meeting_id, passcode, and optional display_name for call-site symmetry.",
  "_migration_note": "v2: Typed Flow format (role/produces/consumes). v1 used no roles.",
  "context": {
    "meeting_url": "https://www.microsoft.com/microsoft-teams/join-a-meeting",
    "meeting_id": "",
    "passcode": "",
    "display_name": ""
  },
  "steps": [
    {
      "id": "prime_browser_session",
      "role": "gate",
      "op": "core:include",
      "params": {
        "fragment": "fragments/browser-session-start.json"
      }
    },
    {
      "id": "teams_join_flow",
      "role": "gate",
      "op": "core:include",
      "params": {
        "fragment": "fragments/microsoft-teams-join-button.json",
        "context": {
          "meeting_url": "{{meeting_url}}",
          "meeting_id": "{{meeting_id}}",
          "passcode": "{{passcode}}",
          "display_name": "{{display_name}}"
        }
      }
    }
  ]
}
