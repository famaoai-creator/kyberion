{
  "description": "Test-only fixture for scripts/run_pipeline.test.ts core:include coverage. Not a production fragment.",
  "context": { "fragment_default": "fragment default" },
  "steps": [
    {
      "op": "core:transform",
      "params": {
        "input": "{{greeting}}",
        "script": "return String(input) + ' from fragment';",
        "export_as": "fragment_result"
      }
    }
  ]
}
