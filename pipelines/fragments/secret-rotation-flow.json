{
  "action": "pipeline",
  "name": "secret-rotation-flow-fragment",
  "version": "2.0.0",
  "description": "Requests a governed human review for secret rotation. The OS keychain stores no expiry metadata, so expiry cannot be inferred safely by the pipeline.",
  "_migration_note": "v2: Typed Flow format (role/produces/consumes). v1 used type + export_as.",
  "context": {
    "service_id": "github"
  },
  "steps": [
    {
      "id": "request_rotation_review",
      "role": "sink",
      "op": "approval:request_review",
      "params": {
        "topic": "Review secret rotation for {{service_id}}",
        "idempotency_key": "secret-rotation-{{service_id}}"
      }
    }
  ]
}
