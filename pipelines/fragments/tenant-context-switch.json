{
  "action": "pipeline",
  "name": "tenant-context-switch-fragment",
  "version": "2.0.0",
  "description": "Switches the active operational context to a specific tenant, loading their rules and identifying boundaries.",
  "_migration_note": "v2: Typed Flow format (role/produces/consumes). v1 used type + export_as.",
  "context": {
    "tenant_slug": "default"
  },
  "steps": [
    {
      "id": "validate_tenant",
      "role": "source",
      "op": "system:exec",
      "produces": { "channel": "tenant_dir_check", "type": "ShellResult" },
      "params": {
        "command": "ls",
        "args": ["knowledge/confidential/{{tenant_slug}}"],
        "allow_error": true
      }
    },
    {
      "id": "switch_context",
      "role": "sink",
      "op": "system:log",
      "params": {
        "message": "Switched to tenant context: {{tenant_slug}}. Enforcement of Tier Guard for this tenant is now active."
      }
    }
  ]
}
