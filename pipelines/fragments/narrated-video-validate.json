{
  "action": "pipeline",
  "name": "narrated-video-validate-fragment",
  "version": "1.0.0",
  "description": "Reusable fragment: validate a narrated video artifact through the video-composition actuator — checks file presence, streams, black frames, key frames, and audio/video duration alignment. Required context vars: narration_path, video_output_path, video_bundle_dir, mission_evidence_dir, video_slug.",
  "steps": [
    {
      "id": "validate_narrated_video_artifact",
      "role": "gate",
      "op": "video-composition:validate_narrated_video_artifact",
      "produces": { "channel": "verify_result", "type": "NarratedVideoArtifactValidationResult" },
      "params": {
        "narration_path": "{{narration_path}}",
        "video_output_path": "{{video_output_path}}",
        "video_bundle_dir": "{{video_bundle_dir}}",
        "mission_evidence_dir": "{{mission_evidence_dir}}",
        "video_slug": "{{video_slug}}",
        "tolerance_sec": 5
      }
    }
  ]
}
