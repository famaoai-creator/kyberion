{
  "pipeline_id": "browser-profile-list",
  "version": "2.0.0",
  "description": "Fragment demonstrating how to list available Kyberion managed profiles and Chrome native profiles.",
  "_migration_note": "v2: Typed Flow format (role/produces/consumes). v1 used type + export_as.",
  "action": "pipeline",
  "options": {
    "headless": true
  },
  "steps": [
    {
      "role": "source",
      "op": "list_profiles",
      "produces": { "channel": "available_profiles", "type": "ProfileList" },
      "params": {}
    },
    {
      "role": "sink",
      "op": "log",
      "params": {
        "message": "Profiles retrieved. They are available in ctx.available_profiles."
      }
    }
  ]
}
