{
  "pipeline_id": "kyberion-config-provisioner",
  "version": "2.0.0",
  "name": "Kyberion Actuator 設定プロビジョナー",
  "description": "組織プロファイルから接続設定を抽出し、エンドポイントの雛形を作成します。",
  "_migration_note": "v2: Typed Flow format (role/produces/consumes). v1 used type + export_as.",
  "action": "pipeline",
  "steps": [
    {
      "id": "read-profile",
      "role": "source",
      "op": "system:read_file",
      "produces": { "channel": "profile_content", "type": "ReadResult" },
      "params": {
        "path": "knowledge/public/tmp-profile.md"
      }
    },
    {
      "id": "analyze-provisioning-needs",
      "role": "transform",
      "op": "reasoning:analyze",
      "consumes": "profile_content",
      "produces": { "channel": "config_drafts", "type": "ConfigDrafts" },
      "params": {
        "instruction": "組織プロファイルの内容 {{profile_content}} を分析し、有効化すべきActuator（Notion, Slack, GitHub）の設定JSON（雛形）を生成せよ。各JSONにはプレースホルダーを含め、ユーザーがAPIキーを入れるべき場所を明示せよ。"
      }
    },
    {
      "id": "propose-configs",
      "role": "sink",
      "op": "system:log",
      "consumes": "config_drafts",
      "params": {
        "message": "\n--- Kyberion Configuration Proposals ---\n\nプロファイルに基づき、以下の設定ファイルを準備しました。内容を確認し、秘密情報をVaultに登録してください。\n\n{{config_drafts}}\n\n※ これらの雛形は `active/shared/tmp/provisioning/` に出力されました。承認後、`knowledge/product/orchestration/service-endpoints/` に配置されます。"
      }
    }
  ]
}
