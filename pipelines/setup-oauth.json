{
  "name": "setup-oauth",
  "action": "pipeline",
  "description": "Interactive pipeline to setup OAuth connection for a specific service",
  "steps": [
    {
      "op": "core:run_oauth_setup",
      "params": { "service_name": "{{service_name}}" }
    }
  ]
}
