name: Document Analysis Pipeline
description: Classify and analyze documents
steps:
  - skill: domain-classifier
    args: '--input "{{input}}"'
    output: domain
  - skill: doc-type-classifier
    args: '--input "{{input}}"'
    output: docType
  - skill: intent-classifier
    args: '--input "{{input}}"'
    output: intent
  - skill: sensitivity-detector
    args: '--input "{{input}}"'
    output: sensitivity
