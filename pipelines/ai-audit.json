{
  "pipeline_id": "ai-audit",
  "version": "1.0.0",
  "description": "AI audit test layer (KC-05): fan tests_ai/*.md semantic invariants out to the reasoning backend and aggregate report.json under active/shared/tmp/ai-audit/. Skips with an explicit report on stub backend; fails when any case fails.",
  "action": "pipeline",
  "schedule": {
    "id": "ai-audit-weekly",
    "cron": "30 5 * * 1",
    "timezone": "Asia/Tokyo",
    "enabled": true
  },
  "steps": [
    {
      "id": "run-ai-audit",
      "role": "sink",
      "op": "core:run_ai_audit",
      "comment": "Typed library entry preserves backend discovery, explicit stub skip, report persistence, and bounded LLM fan-out.",
      "params": {
        "concurrency": 3
      }
    }
  ]
}
