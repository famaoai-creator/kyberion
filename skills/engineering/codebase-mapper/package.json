{
  "name": "codebase-mapper",
  "version": "0.1.0",
  "description": "Maps the directory structure of the project to help the AI understand the codebase layout.",
  "main": "../../../dist/skills/engineering/codebase-mapper/src/index.js",
  "types": "dist/index.d.ts",
  "author": "Gemini Agent",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "test": "vitest run"
  },
  "dependencies": {
    "@agent/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "@types/node": "^20.0.0",
    "vitest": "^1.2.0",
    "@agent/core": "workspace:*"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
