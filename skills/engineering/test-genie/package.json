{
  "name": "test-genie",
  "version": "0.1.0",
  "private": true,
  "description": "Executes the project's test suite and returns the output for AI analysis.",
  "main": "../../../dist/skills/engineering/test-genie/src/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "test": "vitest run"
  },
  "author": "Gemini Agent",
  "license": "MIT",
  "dependencies": {
    "@agent/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "@types/node": "^20.0.0",
    "vitest": "^1.2.0",
    "@types/js-yaml": "^4.0.0",
    "@agent/core": "workspace:*"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
