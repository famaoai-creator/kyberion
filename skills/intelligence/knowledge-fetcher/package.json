{
  "name": "knowledge-fetcher",
  "version": "0.1.0",
  "description": "Fetch knowledge from both public and confidential directories.",
  "main": "../../../dist/skills/intelligence/knowledge-fetcher/src/index.js",
  "private": true,
  "author": "Gemini Agent",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "test": "vitest run"
  },
  "dependencies": {
    "@agent/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "@types/node": "^20.0.0",
    "vitest": "^1.2.0",
    "@agent/core": "workspace:*"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
