{
  "name": "data-anonymizer",
  "version": "1.0.0",
  "private": true,
  "description": "Masks sensitive fields in JSON data for safe sharing.",
  "main": "../../../dist/skills/utilities/data-anonymizer/src/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "test": "vitest run"
  },
  "dependencies": {
    "@agent/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "@types/node": "^20.11.16",
    "vitest": "^1.2.2",
    "@types/yargs": "^17.0.32",
    "@agent/core": "workspace:*"
  },
  "author": "Gemini Agent",
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  }
}
