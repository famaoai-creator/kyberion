# Changelog

> **Status source**: this file is a release history, not the current improvement-plan status. Use [`docs/developer/improvement-plans-2026-08/README.ja.md`](docs/developer/improvement-plans-2026-08/README.ja.md) and each plan's implementation-status section for current state.

All notable changes to Kyberion are documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and the project follows [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

This file is partly auto-generated by `scripts/generate_changelog.ts` from Conventional Commit messages on `main` since the previous tag. See `docs/developer/RELEASE_OPERATIONS.md` for the release process.

## [Unreleased]

### Added — Productization roadmap (Phase A / B / C' / D')

- **Customer aggregation point** (`customer/`, `customer-resolver.ts`) for FDE / implementation-support engagements. Per-customer config under `customer/{slug}/` with overlay precedence over `knowledge/personal/`. See `docs/developer/CUSTOMER_AGGREGATION.md`.
- **Contract semver enforcement** (`scripts/check_contract_semver.ts`) detects breaking changes to actuator surfaces without a version bump. Initial baseline of 26 actuators recorded.
- **Trace persistence** (`persistTrace`, `finalizeAndPersist`, `traceLogDir`) — actuator traces now write to `active/shared/logs/traces/` (or `customer/{slug}/logs/traces/` when active). Browser-actuator and mission_controller checkpoint integrated.
- **Mission lifecycle hardening** (`resumeMission` TOCTOU fix + 60s RESUME idempotency window).
- **Error classifier** (`libs/core/error-classifier.ts`) — maps unstructured errors to a small set of categories with remediation hints.
- **License audit** (`scripts/license_audit.ts`) — scans 3rd-party deps and writes `docs/legal/third-party-licenses.json`.
- **Cross-OS CI matrix** (`.github/workflows/cross-os.yml`) — Ubuntu + macOS + Docker.
- **Golden output check** (`scripts/check_golden_output.ts`) — semantic regression detection for stable pipelines.
- **Chaos drill pipelines** (`pipelines/chaos-*.json`) — recurring failure injection for actuator-down / network-partition / secret-missing.
- **Vertical mission seed templates** (`templates/verticals/{finance-ringi-approval, lifestyle-reservation, it-saas-inventory}/`).
- **OSS governance documents**: `MAINTAINERS.md`, `CODEOWNERS`, `CODE_OF_CONDUCT.md`, `SECURITY.md`, `GOVERNANCE.md`.
- **Public-facing docs**: `docs/WHY.md` (en/ja), `docs/PRIVACY.md` (en/ja), `docs/operator/DEPLOYMENT.md`, `docs/developer/EXTENSION_POINTS.md`, `docs/developer/CUSTOMER_AGGREGATION.md` (en/ja), `docs/developer/MISSION_LIFECYCLE_AUDIT.md`, `docs/developer/TRACE_MIGRATION_TEMPLATE.md`, `docs/developer/CHAOS_DRILLS.md`, `docs/developer/GOLDEN_OUTPUT_CHECK.md`, `docs/PRODUCTIZATION_ROADMAP.md`.

### Changed

- `package.json`: added `check:contract-semver`, `check:golden`, `license:audit` scripts; integrated `check:contract-semver` into `validate`.
- `.gitignore`: added customer aggregation rules.

### Stability

- Customer aggregation API and slug pattern are **Stable v1**.
- Trace types are **Beta** (will be lifted to Stable v1 when remaining actuators migrate per Phase B-1).
- Plugin / Skill format remain **Beta**.

### Security / migration notes

- **`KYBERION_ALLOW_UNAUTH_REMOTE` now parses the full boolean vocabulary.** Chronos Mirror's API guard (`presence/displays/chronos-mirror-v2/src/lib/api-guard.ts`) reads this flag through the registered env accessor, which accepts `1`, `true`, `yes` and `on` (case-insensitive); it previously matched only the literal string `true`. **Affected**: any deployment that set the flag to a truthy value other than `true` — it used to be a silent no-op that failed closed, and it now genuinely enables unauthenticated remote readonly access — but only when neither `KYBERION_API_TOKEN` nor `KYBERION_LOCALADMIN_TOKEN` is configured; token-configured deployments are unaffected. **Action**: audit the value of `KYBERION_ALLOW_UNAUTH_REMOTE` in every environment and unset it (or set it to `false`) unless unauthenticated remote reads are intended.
- **Forwarded client-IP headers are no longer trusted by default (`KYBERION_TRUST_PROXY`).** Chronos Mirror and Concierge (`presence/displays/concierge/src/lib/viewer-context.ts`) ignore `x-forwarded-for` / `x-real-ip` when determining whether a request is loopback, unless the new `KYBERION_TRUST_PROXY` flag is enabled (default off). This closes a header-spoofing path to local-admin privileges. **Affected**: local deployments served through a reverse proxy, which were previously treated as loopback and may now require a token. **Action**: set `KYBERION_TRUST_PROXY=1` only when a trusted reverse proxy overwrites those headers on every inbound request; otherwise leave it unset and issue a surface token instead.

- **Surface viewers never receive the `personal` tier.** Chronos Mirror and Concierge now resolve a registered `localadmin` token that omits `tier_access` to the masked role default (`confidential`, `public`) instead of rejecting it (Chronos previously returned 403 for such registrations), and reject a registration that explicitly requests `personal` with 403. **Affected**: hand-written token registrations that list `personal` in `tier_access`. **Action**: remove `personal` from `tier_access`; personal-tier access is only available to local sovereign processes.
- **Loopback auto-admin requires a trusted proxy on Next.js 15+.** `NextRequest.ip` no longer exists, so without `KYBERION_TRUST_PROXY=1` (behind a proxy that sets `x-real-ip`/`x-forwarded-for`) a browser on the same machine is not detected as loopback and must present a surface token even when `KYBERION_LOCALHOST_AUTOADMIN` is enabled.

### Migration required

- No `migration/<id>.ts` script is required for the current unreleased changes; the operator actions are listed under Security / migration notes above. Release prep must keep this section explicit: use `None` when no migration is required, or list each `migration/<id>.ts` script with the operator impact and rollback note.

---

## How releases are tagged

We use Conventional Commits (`feat:`, `fix:`, `chore:`, `docs:`, `refactor:`, `test:`, `breaking:`).

- `feat:` and `feat(scope):` → minor bump (or major if `BREAKING CHANGE:` footer)
- `fix:` → patch bump
- `breaking:` or `BREAKING CHANGE:` footer → major bump
- everything else → no bump (rolled into the next minor/patch)

When a release is tagged, `scripts/generate_changelog.ts` populates a new section above with the commits since the previous tag.

## Past releases

_None yet — pre-1.0 development. The first tagged release will be v0.1.0._

> **状態の扱い**: CHANGELOG はリリース履歴であり、改善計画の現在状態を管理しません。現在の計画状態は [`docs/developer/improvement-plans-2026-08/README.ja.md`](docs/developer/improvement-plans-2026-08/README.ja.md) と各計画文書の「実装状況」節を参照してください。
